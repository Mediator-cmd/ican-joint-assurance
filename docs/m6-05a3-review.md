# M6-5A-3 六事件连续空间总验收审查

## 1. 审查结论

M6-5A-3 已完成。既有六条有限、一次性的匿名合成事件已经连续通过空间投影、仅规则问答、候选采用/保留、SQLite 重启恢复、SSE 中断后的 REST 恢复、最终完成态和不回绕验收。没有增加随机事件、循环播放、地图专用时钟、前端规划器或第二份运行快照。

> 仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令。

## 2. 连续事件与人工决定

- 独立 SQLite 验收按 08:00、08:04、08:16、08:28、08:40、08:52 连续处理六事件，交替采用和保留候选；六个候选 ID、六个事件 ID 以及不包含事件 ID 的六组 `(事件类型, 主区域, 真实候选变化任务)` 空间签名均唯一。
- 每次等待确认都投影 10 条当前实线、5 个资源和 6 个事件。候选虚线集合严格等于 `changed_candidate_task_ids`，允许真实变化为 0，不为视觉效果伪造路线。
- 采用后当前方案精确切换到候选；保留后当前方案不变。两类人工决定均清除候选路线并把当前事件转为 `resolved`，采用前当前实线始终保留。
- 每个事件的仅规则问答只匹配当前事件，首条引用为对应 `SPATIAL-FACT-EVT-*`，聚焦对象全部受已引用事实支持；问答前后去除瞬时 `server_time` 的业务快照完全一致。

## 3. 恢复、陈旧态与完成态

- 第三事件等待确认时重建 FastAPI 应用和 SQLite repository，session、revision、仿真时间、候选 ID、事件状态和完整空间视图原样恢复。
- 新 revision 产生后，旧 revision 的空间视图和空间问答均返回 409；前端既有 revision/layout 失效逻辑继续清除旧回答和 AI 高亮。
- 真实浏览器故障注入中，SSE 连续失败后页面稳定显示“快照恢复”，每 2 秒读取 REST 权威快照；EventSource 后续 `onopen/onerror` 不再把 polling 状态覆盖成抖动的“正在恢复”。断线期间地图保持同一 revision 和仿真时间，不自推进；解除阻断后恢复实时同步。
- 正式会话 `RUN-2639C8D8CCC848A6` 经标准脚本从停止状态恢复到第六事件 `V7 / R63 / 08:52 / awaiting_confirmation`。第六事件规则问答只匹配 `EVT-SIM218-GATE-RETURN`，引用 5 个受影响任务并聚焦 3 个资源和 3 个匿名区域，运行快照不变。
- 第六候选 `PLAN-2639C8D8CCC848A6-R62-CP-SAT` 被人工采用后，R64 当前方案等于该候选、候选清除、第六事件 resolved、空间视图仍为 10 条当前路线和 0 条候选路线。继续运行后在 R66 精确到达 `V7 / 09:30 / completed`；等待后 revision、时间、6 个事件和空间视图均不变化，没有第七事件或回绕。

## 4. 浏览器与响应式证据

- 本轮真实桌面页面在 R63 显示 08:52、V7、10/10 任务、5/5 资源、6/6 事件、第六事件待确认、10 条当前路线和 0 条候选路线；完整安全声明可见。
- 正常在线首次加载只有 React 开发提示，0 error、0 warning。验收结束按标准脚本停止服务后，仍未关闭的浏览器才记录预期的连接拒绝，这些停机日志不计为正常在线控制台结果。
- M6-5A-3 未修改 DOM、CSS、地图 SVG 或问答布局。390×844 的直接布局证据沿用 M6-5A-2 对同一界面代码的结果：document、问答面板和回答区 `clientWidth == scrollWidth`，回答自然展开且 `clientHeight == scrollHeight`。本轮尝试重新执行移动端浏览器检查时，本机浏览器安全策略拒绝访问回环地址，因此没有把该次未执行检查描述为新证据。

## 5. 自动验证

- M6-5A-3 连续空间验收：`1 passed`；覆盖六事件、重启恢复、旧 revision 409、采用/保留、完成态和不回绕。
- 后端完整集合：`306 passed in 36.76s`。
- 前端：8 个测试文件、`42 passed`；TypeScript 类型检查通过。
- Vite 生产构建：1797 个模块，CSS 90.56 kB（gzip 17.47 kB），JS 341.40 kB（gzip 98.92 kB）。
- Python `compileall` 通过；`pip check` 返回 `No broken requirements found.`；`git diff --check` 通过；非测试文件真实 key-like 扫描为 0。
- 标准停止脚本精确停止前端 PID 15184 和后端 PID 7804，并删除运行状态；服务日志无 5xx、Traceback 或异常退出。

## 6. 边界与下一入口

M6-5A 的 2D 空间态势、方案路径、AI 进程问答和连续事件总验收已经完成。可选 M6-5A-4 3D 不是首次 GitHub/公网发布前置条件，不应阻塞当前产品收口。

下一唯一入口为 M6-6：统一复核冻结规模报告、故障恢复、无模型路径、真实模型只读边界、浏览器体验、单 origin 容器和干净部署，并形成 M6 总结论。M6-6 不创建 GitHub 远程、不发布公网地址；这些外部动作仍属于 M7，需用户单独授权。
