# 联保智调项目进度与交接记录

> 这份文档是项目的持续交接入口。每完成一个工作单元，都要更新“当前状态”“已完成工作”“验证记录”“问题与风险”和“下一步任务”。新窗口开始工作时，先读本文件，再读 `docs/project-plan.md`，最后检查 Git 状态。

## 1. 文档元数据

- 项目：联保智调
- 副标题：航班扰动下特殊旅客地面保障资源优化与推演平台
- 参赛方向：2026 年 iCAN AI 应用创新挑战赛，软件赛道，交通出行方向
- 当前日期：2026-07-27
- 官方作品提交截止：2026-09-30
- 当前阶段：M1 数据模型与仿真场景（待开始）
- 文档状态：持续更新
- 主要规划：[project-plan.md](project-plan.md)
- 本文档维护原则：只记录已确认事实；候选方案必须标注“候选”；问题解决后保留原因和验证证据。

## 2. 当前项目状态

### 2.1 已确认的产品定位

联保智调是面向机场地面保障人员的仿真与辅助决策系统。它在航班延误、登机口变更、摆渡资源减少等扰动下，对摆渡车、轮椅、服务人员和特殊旅客服务任务进行约束调度，输出可执行、可解释、可复核的任务清单。

### 2.2 已确认的核心价值

1. 硬约束由确定性调度逻辑处理，包括资源容量、服务能力、时间窗和任务不重叠。
2. AI 只负责自然语言事件理解、字段缺失追问和结果解释，不直接生成未经校验的调度结果。
3. 新事件进入后执行滚动重规划，并保留调整前后指标和审计记录。
4. 通过同一仿真场景对比 FIFO 基线与优化方案，量化关键任务逾期、等待时间和资源利用率变化。

### 2.3 明确不做的内容

- 不接管真实机场生产系统。
- 不提供真实放行、登机、改签或车辆控制指令。
- 不保存姓名、证件号、联系方式等旅客个人信息。
- 不把仿真或历史回放数据包装成真实生产数据。

所有界面、报告和演示材料应保留“仅供教学仿真与辅助决策使用，不构成真实机场运行指令”的安全提示。

## 3. 已完成工作

### 2026-07-27 / M0

- 阅读并确认竞赛通知中的赛道、参赛限制、创新方向和作品提交要求。
- 确定项目名称“联保智调”和交通出行方向的特殊旅客地面保障资源调度主题。
- 创建总体规划文档 `docs/project-plan.md`，冻结 MVP、技术路线、数据模型、调度边界、测试指标和 M0-M7 里程碑。
- 创建单仓库目录骨架：`frontend/`、`backend/`、`data/scenarios/`、`tests/`、`scripts/`、`docs/scenarios/`。
- 创建第一版 `README.md` 和 `.gitignore`。
- 初始化 Git 仓库并设置仓库级 Git 身份（见第 6 节）。
- 提交 M0 基线 commit：`65b9e81 chore: initialize project workspace`。

## 4. 当前阶段说明：M0

### 目标

冻结需求边界，建立可持续开发和交接的仓库基线，避免在数据模型和调度内核尚未稳定时直接堆叠 AI 或前端功能。

### 验收结果：已完成

- 仓库能够通过 README 说明项目定位、安全边界、目录和开发顺序。通过。
- 进度文档能够让新窗口在不依赖本次对话的情况下恢复项目状态。通过。
- Git 工作区只包含本阶段预期文件，且没有密钥、`.env` 或个人信息。通过。
- 下一阶段入口明确为 M1 数据模型与最小仿真场景。通过。

## 5. 已确认的技术决策

| 决策 | 当前结论 | 原因/约束 |
| --- | --- | --- |
| 架构 | React + TypeScript 前端，FastAPI + Pydantic 后端 | 便于快速演示、数据校验和接口测试 |
| 调度顺序 | 先 FIFO 基线，再做约束优化 | 先建立可解释的对照组，再量化优化收益 |
| 优化器 | 候选为 OR-Tools CP-SAT 或线性规划 | 适合资源、时间窗和不重叠约束；M2 决定最终模型 |
| 数据库 | 开发阶段 SQLite | 降低单人开发和演示环境成本，保留 PostgreSQL 适配 |
| AI 边界 | 解析和解释，不直接决定排班 | 防止幻觉绕过硬约束，保证结果可复核 |
| 数据来源 | 匿名化、历史回放或仿真数据 | 不依赖真实机场生产授权，保护个人信息 |
| 首个场景 | 多航班扰动下特殊旅客地面保障资源分配 | 与竞赛交通出行方向和实用性问题直接对应 |

## 6. GitHub 与本地环境状态

- GitHub CLI 账号：`Mediator-cmd`（用户已完成网页登录和验证码验证）。
- GitHub 用户 ID：`247509064`。
- `gh auth setup-git` 已执行，HTTPS 凭据助手已配置。
- GitHub 访问验证：`git ls-remote https://github.com/git/git.git HEAD` 已成功。
- 当前环境曾设置无响应代理 `127.0.0.1:9`；访问 GitHub 的命令需要临时清除 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 及其小写变量。
- 全局 Git 身份保持用户原设置不变；本仓库使用仓库级身份，避免影响其他项目。
- GitHub 远程仓库：尚未创建，待确认仓库名称后再创建并添加 `origin`。

## 7. 验证记录

| 日期 | 检查项 | 结果 | 证据/备注 |
| --- | --- | --- | --- |
| 2026-07-27 | 读取 `project-plan.md` | 通过 | 规划文档存在，UTF-8 内容可正常读取 |
| 2026-07-27 | 仓库目录骨架 | 通过 | `frontend/`、`backend/`、`data/scenarios/`、`tests/`、`scripts/` 和 `docs/scenarios/` 已创建 |
| 2026-07-27 | README 与进度文档 | 通过 | 已检查项目定位、安全边界、当前阶段和恢复说明 |
| 2026-07-27 | Git 初始化与仓库级身份 | 通过 | `main` 分支；工作区干净；本地身份为 `Mediator-cmd` / `247509064+Mediator-cmd@users.noreply.github.com` |
| 2026-07-27 | M0 基线提交 | 通过 | commit `65b9e81` |

## 8. 问题、风险与处理方式

### 已知问题

1. 项目尚未创建 GitHub 远程仓库，原因是仓库名称尚未最终确认。
2. 前后端依赖尚未安装，当前仓库不能启动业务服务；这是 M0 的预期状态。
3. 真实机场数据接入需要数据授权、脱敏和接口适配，不作为比赛 MVP 的前置条件。

### 风险控制

- 不把外部 API 密钥写入代码或文档。
- 不在日志、截图、演示视频中出现可识别个人信息。
- 对 AI 解析结果执行 Pydantic 校验和硬约束校验；模型不可用时保留规则回退。
- 任何性能指标均标注为仿真测试目标，不外推为真实机场生产承诺。

## 9. 下一步任务：M1

1. 确定 Python 版本和后端依赖清单。
2. 定义 `Scenario`、`FlightEvent`、`ServiceTask`、`Resource`、`Zone` 数据模型。
3. 编写一个包含航班延误、服务任务、资源和区域的最小 JSON 仿真场景。
4. 增加数据校验错误示例和最小 pytest 测试。
5. 暂时使用 FIFO 规则输出任务分配结果，不接入 AI 和复杂优化器。
6. 完成后更新本文件、README 和 Git commit，再进入 M2。

## 10. 新窗口恢复说明

新窗口接续本项目时，按以下顺序执行：

1. 阅读 `docs/project-progress.md` 全文，确认当前阶段、已知问题和下一步任务。
2. 阅读 `docs/project-plan.md`，确认需求边界和验收标准。
3. 执行 `git status --short --branch`，不要覆盖工作区已有改动。
4. 检查“已完成工作”末尾的最新日期记录和“验证记录”。
5. 只从“下一步任务”开始推进；完成后在本文档末尾追加新的日期记录。

## 11. 更新模板

后续每次完成一个工作单元，在“已完成工作”末尾追加：

```markdown
### YYYY-MM-DD / 阶段或任务名

- 完成：
- 关键文件：
- 验证：
- 已知问题/风险：
- 下一步：
```

不要删除历史记录；若决策发生变化，在“技术决策”表中更新当前结论，并在更新记录中说明变更原因。
