# 联保智调项目进度与交接记录

> 这份文档是项目的持续交接入口。每完成一个工作单元，都要更新“当前状态”“已完成工作”“验证记录”“问题与风险”和“下一步任务”。新窗口开始工作时，先读本文件，再读 `docs/project-plan.md`，最后检查 Git 状态。

## 1. 文档元数据

- 项目：联保智调
- 副标题：航班扰动下特殊旅客地面保障资源优化与推演平台
- 参赛方向：2026 年 iCAN AI 应用创新挑战赛，软件赛道，交通出行方向
- 当前日期：2026-08-09
- 官方作品提交截止：2026-09-30
- 当前阶段：M6 规模测试、体验收敛与部署准备（M6-0 至 M6-4 已完成；下一单元 M6-5）
- 文档状态：持续更新
- 主要规划：[project-plan.md](project-plan.md)
- 本文档维护原则：只记录已确认事实；候选方案必须标注“候选”；问题解决后保留原因和验证证据。

## 2. 当前项目状态

### 2.1 已确认的产品定位

联保智调是面向机场地面保障人员的仿真与辅助决策系统。它在航班延误、登机口变更、摆渡资源减少等扰动下，对摆渡车、轮椅、服务人员和特殊旅客服务任务进行约束调度，输出可执行、可解释、可复核的任务清单。

### 2.2 已确认的核心价值

1. 硬约束由确定性调度逻辑处理，包括资源容量、服务能力、时间窗和任务不重叠。
2. AI 只负责自然语言事件理解、字段缺失追问和结果解释，不直接生成未经校验的调度结果。
3. 新事件进入后执行滚动重规划，并保留调整前后指标和审计记录。
4. 通过同一仿真场景对比 FIFO 基线与优化方案，量化关键任务逾期、等待时间和资源利用率变化。
5. M4 必须实现后端权威仿真时间、任务与资源状态自动转换、事件到时生效、滚动重规划、人工确认和页面实时更新，不能用固定快照切换替代运行过程。

### 2.3 明确不做的内容

- 不接管真实机场生产系统。
- 不提供真实放行、登机、改签或车辆控制指令。
- 不保存姓名、证件号、联系方式等旅客个人信息。
- 不把仿真或历史回放数据包装成真实生产数据。

所有界面、报告和演示材料应保留“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令。”的安全提示。

## 3. 已完成工作

### 2026-07-27 / M0

- 阅读并确认竞赛通知中的赛道、参赛限制、创新方向和作品提交要求。
- 确定项目名称“联保智调”和交通出行方向的特殊旅客地面保障资源调度主题。
- 创建总体规划文档 `docs/project-plan.md`，冻结 MVP、技术路线、数据模型、调度边界、测试指标和 M0-M7 里程碑。
- 创建单仓库目录骨架：`frontend/`、`backend/`、`data/scenarios/`、`tests/`、`scripts/`、`docs/scenarios/`。
- 创建第一版 `README.md` 和 `.gitignore`。
- 初始化 Git 仓库并设置仓库级 Git 身份（见第 6 节）。
- 提交 M0 基线 commit：`65b9e81 chore: initialize project workspace`。

### 2026-07-27 / M1

- 确认开发环境为 Python 3.13.9、Pydantic 2.12.5、pytest 8.0.0。
- 定义 `Scenario`、`Flight`、`FlightEvent`、`ServiceTask`、`Resource`、`Zone` 及相关枚举。
- 所有持久化模型采用严格字段校验，拒绝未知字段、重复 ID、无时区时间、非法时间窗和未知跨实体引用。
- 创建匿名化仿真场景 `data/scenarios/terminal-disturbance-demo.json`，包含 3 个区域、2 个航班、2 个扰动事件、4 个服务任务和 5 个资源。
- 创建场景导入器和命令行验证脚本，提供合法场景摘要和非法输入错误信息。
- 添加 6 个自动化测试，覆盖合法导入、未知航班、重复资源、未知移动区域、无时区时间和损坏 JSON。
- 记录场景语义：`flights`/`tasks` 为基线状态，`events` 为后续按时间注入的扰动流。
- 将示例航班改为 `SIM102`、`SIM218` 虚构编码，并要求场景显式声明 `data_classification: synthetic`，防止与真实运行数据混淆。
- 提交 M1 基线 commit：`3ecbabe feat: add validated simulation scenario models`。

### 2026-07-27 / M2

- 最终展示形态确定为浏览器运行态控制台，演示蓝图见 `docs/demo-blueprint.md`。
- 定义 `Assignment`、`Plan`、`PlanMetric`、未分配原因和硬约束违规模型。
- 实现确定性 FIFO 基线、区域最短路径、事件深拷贝应用和独立硬约束检查器。
- 使用 Google OR-Tools CP-SAT 实现分层目标优化：先保障 P1，再最大化任务总数和优先级得分，最后降低等待与移动成本。
- 将演示场景扩展为 5 个任务、4 个资源，通过单轮椅时间窗冲突形成可解释的算法对比。
- FIFO 结果为完成 4/5、P1 完成率 50%；事件后 CP-SAT 结果为完成 5/5、P1 完成率 100%；两者硬约束违规均为 0。
- 创建 React + TypeScript + Vite 运行态网站雏形，支持“扰动前 FIFO”“事件后 FIFO”“CP-SAT 优化”三种视图。
- 网站直接读取 Python 导出的 `frontend/public/demo-output.json`，不在前端重新计算核心指标。
- 完成桌面 1440×1000 和移动 390×844 浏览器验证，模式切换正确，移动页面无整体横向溢出。
- OR-Tools 安装在项目 `.venv`；全局 Anaconda 已恢复原 protobuf 5.29.3，避免影响其他项目。
- 提交 M2 基线 commit：`0759cbe feat: add scheduling engine and dashboard prototype`。

## 4. 阶段验收记录

### M0：需求冻结与仓库初始化

#### 目标

冻结需求边界，建立可持续开发和交接的仓库基线，避免在数据模型和调度内核尚未稳定时直接堆叠 AI 或前端功能。

#### 验收结果：已完成

- 仓库能够通过 README 说明项目定位、安全边界、目录和开发顺序。通过。
- 进度文档能够让新窗口在不依赖本次对话的情况下恢复项目状态。通过。
- Git 工作区只包含本阶段预期文件，且没有密钥、`.env` 或个人信息。通过。
- 下一阶段入口明确为 M1 数据模型与最小仿真场景。通过。

### M1：数据模型与仿真场景

#### 目标

建立可复用、可校验的仿真数据边界，使后续调度内核只接收结构完整且引用有效的场景。

#### 验收结果：已完成

- 能导入包含航班扰动、服务任务、区域和资源的完整 JSON 场景。通过。
- 能报告损坏 JSON、重复 ID、非法时间和未知关联对象。通过。
- 场景不包含姓名、证件号、联系方式等个人信息。通过。
- 具备命令行验证入口和自动化回归测试。通过，`6 passed`。

### M2：调度内核与网站雏形

#### 目标

建立可复核的 FIFO 对照组和 CP-SAT 优化方案，并让评委通过浏览器直接查看事件、任务、资源、指标和方案取舍。

#### 验收结果：已完成

- FIFO、CP-SAT 和事件后重规划均由同一场景模型驱动。通过。
- 所有计划经过独立硬约束复核，演示方案违规数为 0。通过。
- 资源不足、时间窗冲突和优先级取舍均有明确未分配原因。通过。
- React 网站能切换三种方案并显示未分配任务。通过。
- 后端测试、前端类型检查、生产构建和桌面/移动浏览器检查通过。

### M3：后端业务 API 与前后端联调

#### 目标

将 M2 已验证的调度内核封装为版本化、可审计的业务 API，并让五页工作台通过同一后端契约运行，同时保留明确的离线回退。

#### 验收结果：已完成

- API 可独立完成场景查询、FIFO、结构化事件、CP-SAT、方案比较和审计闭环。通过。
- 不可变基线、期望版本、事件防重复和计划不可变语义通过自动化测试。通过。
- 前端 API 优先、静态回退、来源显示、双失败和恢复闭环通过。通过。
- Windows 一键启停可管理 FastAPI + Vite 服务组并精确停止。通过。
- README、OpenAPI 操作指南、M3 总复盘、交接记录和收口提交完成。通过。
- 完整证据见 `docs/m3-review.md`；M4 动态运行能力不计入本阶段完成范围。

## 5. 已确认的技术决策

| 决策 | 当前结论 | 原因/约束 |
| --- | --- | --- |
| 架构 | React + TypeScript 前端，FastAPI + Pydantic 后端 | 便于快速演示、数据校验和接口测试 |
| 调度顺序 | 先 FIFO 基线，再做约束优化 | 先建立可解释的对照组，再量化优化收益 |
| 优化器 | 候选为 OR-Tools CP-SAT 或线性规划 | 适合资源、时间窗和不重叠约束；M2 决定最终模型 |
| 数据库 | M3 使用进程内仓库；API 契约稳定后接 SQLite | 先验证版本和事件语义，降低单人开发成本，并保留 PostgreSQL 适配 |
| AI 边界 | 解析和解释，不直接决定排班 | 防止幻觉绕过硬约束，保证结果可复核 |
| 数据来源 | 匿名化、历史回放或仿真数据 | 不依赖真实机场生产授权，保护个人信息 |
| 首个场景 | 多航班扰动下特殊旅客地面保障资源分配 | 与竞赛交通出行方向和实用性问题直接对应 |
| 场景主格式 | M1 使用 UTF-8 JSON；CSV 适配按实际导入需求后置 | 场景包含多实体和嵌套引用，JSON 可保留完整结构并减少多文件一致性问题 |
| 场景事件语义 | 航班和任务是基线，事件按时间注入 | 避免导入阶段改写原始数据，便于重规划前后对比 |
| 数据分类标识 | 每个场景必须声明 `synthetic` 或 `anonymized_replay` | 禁止未标明来源的数据进入演示和评测流程 |
| 最终展示形态 | 浏览器运行态控制台 + FastAPI + 可复现仿真数据 | 评委无需查看代码即可完成完整操作和指标比较 |
| 优化器 | OR-Tools CP-SAT，分层固定优先级目标 | 先保障 P1，再考虑总完成数、其他优先级和等待/移动成本 |
| 演示数据流 | Python 生成 JSON，前端只负责展示 | 防止前端与后端指标不一致，M3 原契约替换为 API |
| Python 环境 | 项目 `.venv` 隔离安装 | OR-Tools protobuf 6.x 与全局 `mem0ai` protobuf 5.x 不兼容 |
| M3 场景状态 | 进程内仓库 + 防御性深拷贝 + `RLock` + 期望版本校验 | 保证基线不可变、并发写入原子、过期请求不覆盖新版本；SQLite 后置 |
| 产品差异化 | 工作流、确定性计算和证据链优先，AI 仅做输入与解释增强 | 避免聊天套壳；模型不可用时仍能导入、校验、调度、比较和审计 |
| M4 实时语义 | 可复现的仿真实时运行；REST 控制、SSE 推送、短轮询恢复 | 不冒充真实生产数据；后端维护权威时间和状态，前端不随机生成变化 |
| 动态重规划 | 事件触发时自动暂停；冻结已完成和正在执行任务；候选方案需人工确认 | 保护已经发生的事实，避免每秒无意义求解、候选方案过期或静默替换执行计划 |
| M4 运行契约 | `runtime_models.py` + `m4-runtime-contract.md` 为唯一事实来源 | 后端路由、SQLite、SSE 和前端不得各自创建平行时钟、状态或 revision |
| 实时消息编号 | 持久业务 `revision` 与 SSE `stream_id + sequence` 分离 | tick/heartbeat 不写数据库也不增加 revision，但仍可可靠去重和排序 |
| 会话恢复入口 | SQLite 持久化 + 分页会话列表；重启后运行中会话安全转为暂停 | 数据不仅能保存，还必须能被浏览器重新发现并由用户继续 |
| M4 投影恢复 | 每个新运行会话在 SQLite 保存不可变场景版本、确认方案、完整事件目录和已应用事件版本证据 | 重启后即使 M3 进程内方案对象已消失，也能按同一时间重建完全一致的运行快照 |
| M4 跨版本运行聚合 | 同一 SQLite JSON 聚合同时保存初始场景/方案、当前事件场景、当前确认方案、候选方案、冻结任务和事件决定证据 | 事件后当前场景可先于 active plan；拒绝候选和重启均不能丢失旧方案或已发生事实 |
| M4 实时流 | 每会话一个进程内 `RuntimeStreamBroker`、共享 `stream_id + sequence` 和 256 条环形缓冲 | 消息层只采样权威快照与 SQLite 审计；旧流、缺口或服务重启统一回到完整 snapshot |
| M4 前端状态所有权 | 五页共享一个 `RuntimeSessionSnapshot`；浏览器只拥有连接、轮询和页面选择 | 前端不推进时钟、不推导任务/资源/事件状态、不运行规划器；旧 REST 响应不得覆盖较新的权威状态 |

## 6. GitHub 与本地环境状态

- GitHub CLI 账号：`Mediator-cmd`（用户已完成网页登录和验证码验证）。
- GitHub 用户 ID：`247509064`。
- `gh auth setup-git` 已执行，HTTPS 凭据助手已配置。
- GitHub 访问验证：`git ls-remote https://github.com/git/git.git HEAD` 已成功。
- 当前环境曾设置无响应代理 `127.0.0.1:9`；访问 GitHub 的命令需要临时清除 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 及其小写变量。
- 全局 Git 身份保持用户原设置不变；本仓库使用仓库级身份，避免影响其他项目。
- GitHub 远程仓库：尚未创建，待确认仓库名称后再创建并添加 `origin`。
- 全局 Python：3.13.9（Anaconda），保留 protobuf 5.29.3，不安装 OR-Tools。
- 项目 `.venv`：OR-Tools 9.15.6755、Pydantic 2.13.4、pytest 8.4.2、FastAPI 0.140.7、Uvicorn 0.51.0、httpx2 2.9.1、protobuf 6.33.6。

## 7. 验证记录

| 日期 | 检查项 | 结果 | 证据/备注 |
| --- | --- | --- | --- |
| 2026-07-27 | 读取 `project-plan.md` | 通过 | 规划文档存在，UTF-8 内容可正常读取 |
| 2026-07-27 | 仓库目录骨架 | 通过 | `frontend/`、`backend/`、`data/scenarios/`、`tests/`、`scripts/` 和 `docs/scenarios/` 已创建 |
| 2026-07-27 | README 与进度文档 | 通过 | 已检查项目定位、安全边界、当前阶段和恢复说明 |
| 2026-07-27 | Git 初始化与仓库级身份 | 通过 | `main` 分支；工作区干净；本地身份为 `Mediator-cmd` / `247509064+Mediator-cmd@users.noreply.github.com` |
| 2026-07-27 | M0 基线提交 | 通过 | commit `65b9e81` |
| 2026-07-27 | Python 语法编译 | 通过 | `python -m compileall -q backend scripts tests` |
| 2026-07-27 | 合法场景导入 | 通过 | 验证脚本输出 3 区域、2 航班、2 事件、4 任务、5 资源 |
| 2026-07-27 | 数据校验测试 | 通过 | `python -m pytest -q`，`6 passed in 0.12s` |
| 2026-07-27 | 补丁格式检查 | 通过 | `git diff --check` 无错误 |
| 2026-07-27 | M1 基线提交 | 通过 | commit `3ecbabe` |
| 2026-07-27 | M2 后端回归 | 通过 | `.venv\\Scripts\\python.exe -m pytest -q`，`19 passed` |
| 2026-07-27 | M2 演示数据 | 通过 | FIFO `4/5`、P1 `50%`；CP-SAT `5/5`、P1 `100%`；违规 `0` |
| 2026-07-27 | 前端类型检查 | 通过 | `npm run typecheck` |
| 2026-07-27 | 前端生产构建 | 通过 | `npm run build`，Vite 构建成功 |
| 2026-07-27 | 浏览器桌面检查 | 通过 | 1440×1000，全页非空，三视图和未分配任务正确 |
| 2026-07-27 | 浏览器移动检查 | 通过 | 390×844，页面 `scrollWidth = clientWidth = 390` |
| 2026-07-27 | M2 基线提交 | 通过 | commit `0759cbe` |
| 2026-07-28 | M3-0 契约复核 | 通过 | API、状态码、版本语义和事件防重复边界与 M2 模型兼容 |
| 2026-07-28 | M3-1 后端回归 | 通过 | `.venv\Scripts\python.exe -m pytest -q`，`25 passed` |
| 2026-07-28 | M3-1 依赖与编译 | 通过 | `pip check` 无冲突；`compileall` 通过 |
| 2026-07-28 | Uvicorn HTTP 冒烟 | 通过 | health 200、OpenAPI 200、未知路由统一 404；测试服务已关闭 |
| 2026-07-28 | M3-2 仓库专项测试 | 通过 | 10 项测试覆盖不可变基线、事件目录、版本冲突、重复事件、审计和并发提交 |
| 2026-07-28 | M3-2 全量回归 | 通过 | `.venv\Scripts\python.exe -m pytest -q`，`35 passed`；`compileall` 通过 |
| 2026-07-28 | M3-3 场景接口专项测试 | 通过 | 11 项测试覆盖内置示例、导入、摘要、缺口提示、分页、历史版本和错误引导 |
| 2026-07-28 | M3-3 全量回归 | 通过 | `.venv\Scripts\python.exe -m pytest -q`，`46 passed`；`compileall` 通过 |
| 2026-07-28 | M3-3 Uvicorn 冒烟 | 通过 | 场景列表/详情 200，内置示例可规划且含 5 任务、4 资源、2 待处理事件；服务已关闭 |
| 2026-07-28 | 可理解性 UX 检查点 | 通过 | 默认显示系统建议；桌面/移动无整体横向溢出；方案切换、推荐入口和专业信息展开通过 |
| 2026-07-28 | M3-4 事件与计划专项测试 | 通过 | 13 项 API 工作流测试；分批事件无双重延误、版本冲突原子、计划可查询且解释确定 |
| 2026-07-28 | M3-4 全量回归 | 通过 | `.venv\Scripts\python.exe -m pytest -q`，`61 passed`；`compileall` 通过 |
| 2026-07-28 | M3-4 Uvicorn 闭环 | 通过 | health ok；版本 1→2；FIFO 4/5；优化 5/5、紧急任务 100%、冲突 0；2 个方案可查询；服务已关闭 |
| 2026-07-29 | M3-5 比较/审计/演示专项测试 | 通过 | 6 项测试覆盖完整闭环、差值方向、缺失/跨场景方案、审计限额与脱敏、演示契约 |
| 2026-07-29 | M3-5 全量回归 | 通过 | `.venv\Scripts\python.exe -m pytest -q`，`67 passed`；`compileall` 和 `git diff --check` 通过 |
| 2026-07-29 | M3-5 Uvicorn 闭环 | 通过 | 7 个接口状态为 200/201/200/201/201/200/200；4/5→5/5、紧急保障率 +50 个百分点；8015 端口已释放 |
| 2026-07-29 | M3-6A 前端加载测试 | 通过 | Vitest `4 passed`，覆盖 API 成功、静态回退、双失败和重试恢复 |
| 2026-07-29 | M3-6A 前后端回归 | 通过 | `npm run typecheck`、`npm run build`、后端 `67 passed`、`compileall` 和 `git diff --check` 通过 |
| 2026-07-29 | M3-6A 真实加载链 | 通过 | 后端在线经 Vite 代理 API 200；后端停止后 API 502、静态文件 200；8000/4188 端口已释放 |
| 2026-07-29 | M3-6B 前端联调测试 | 通过 | Vitest `7 passed`，新增 3 项任务/事件/资源选择协调测试；类型检查与生产构建通过 |
| 2026-07-29 | M3-6B 来源恢复闭环 | 通过 | 服务数据 → 本地演示数据 → 服务数据；资源页、原始计划、紧急筛选、`TASK-005`、第二事件与 `BUS-01` 全部保持 |
| 2026-07-29 | M3-6B 响应式浏览器检查 | 通过 | 1440×900 与 390×844 均无文档级溢出，来源标签和完整安全声明可见；仅 1 条预期 API 502 |
| 2026-07-30 | M3-7 双服务启动 | 通过 | 后端健康后再启动前端；健康接口、前端页面和 Vite API 代理均为 200；重复启动复用同一 PID 组 |
| 2026-07-30 | M3-7 Edge 与根入口 | 通过 | `启动联保智调.cmd` 启动双服务并打开 Microsoft Edge；`停止联保智调.cmd` 先停前端再停后端，不关闭浏览器 |
| 2026-07-30 | M3-7 精确停止隔离 | 通过 | 临时无关 Node/Python 进程在项目停止后仍存活；项目状态文件移除，`4173` 与 `8000` 端口释放 |
| 2026-07-30 | M3-7 全量回归 | 通过 | 前端 `7 passed`、类型检查、生产构建；后端 `67 passed`、`compileall`；PowerShell 语法与 `git diff --check` 通过 |
| 2026-07-30 | M3-8 真实 HTTP API | 通过 | 独立 `8019` 完成健康、FIFO、事件、CP-SAT、比较、按 ID 回查、列表和审计闭环；`4/5`→`5/5`，紧急任务 `50%`→`100%`，违规均为 `0`；端口已释放 |
| 2026-07-30 | M3-8 浏览器与恢复 | 通过 | Edge 覆盖 1440×900、390×844 和五页；最小可见字号 14px，无文档级溢出；服务数据→静态回退→服务恢复及双失败重试通过 |
| 2026-07-30 | M3-8 双服务联调 | 通过 | `.runtime` 为 `ready`；后端直连与 Vite 代理健康接口均为 `ok`；临时 API 停止后原 `8000/4173` 仍正常 |
| 2026-07-30 | M3-8 全量回归 | 通过 | 后端 `67 passed`、前端 `7 passed`、类型检查、生产构建、`compileall`、`pip check`、PowerShell 语法和 `git diff --check` 通过 |
| 2026-07-30 | M3-9 OpenAPI 文档 | 通过 | Swagger 含完整安全声明、五组业务标签和可执行工作流说明；独立操作指南覆盖请求体、冲突恢复和 PowerShell 示例 |
| 2026-07-30 | M3-9 真实 OpenAPI 冒烟 | 通过 | 独立 Uvicorn `8020` 下 `/docs` 与 `/api/v1/openapi.json` 均为 200，安全声明、5 组标签和 9 条业务路径正确；端口已释放 |
| 2026-07-30 | M3-9 阶段收口 | 通过 | M3 review、README、实施计划、退出条件和新窗口入口统一指向 M4-0 |
| 2026-07-30 | M4-0 契约专项测试 | 通过 | `tests/test_runtime_contracts.py`，`14 passed`；覆盖时区、倍速、状态不变量、候选方案、会话恢复和 SSE 判别载荷 |
| 2026-07-30 | M4-0 后端回归 | 通过 | 后端全量 `81 passed`，`compileall`、`pip check` 和 `git diff --check` 通过 |
| 2026-07-30 | M4-0 契约审查 | 通过 | REST/SSE、revision/sequence、状态边界、错误码、SQLite 恢复和后续单元职责无歧义 |
| 2026-07-31 | M4-1 后端回归 | 通过 | 后端全量 `95 passed`；权威时钟、CAS、SQLite 和重启恢复通过 |
| 2026-07-31 | M4-2 投影专项测试 | 通过 | `26 passed`；覆盖 ready、边界前/时刻/后一秒、零分钟移动、航班、事件、旧库迁移、无副作用查询和空 M3 仓储重启恢复 |
| 2026-07-31 | M4-2 后端回归 | 通过 | 后端全量 `107 passed`；`compileall`、`pip check` 和 `git diff --check` 通过 |
| 2026-07-31 | M4-2 真实 Uvicorn | 通过 | `15x` 下快照从 `08:00` 推进到 `08:13:39`，任务/资源/航班/事件同步变化而 revision/审计不增加；重启后恢复为 `paused` revision `3` 且完整投影仍可读取；8011 已释放 |
| 2026-07-31 | M4-3 重规划专项测试 | 通过 | `10 passed`；覆盖准确事件边界、同刻批处理、冻结 assignment、跨版本拒绝、手动重算、防陈旧、失败回退、并发和候选跨重启 |
| 2026-08-01 | M4-3 后端回归 | 通过 | 后端全量 `118 passed in 4.41s`；`compileall`、`pip check` 和 `git diff --check` 通过 |
| 2026-07-31 | M4-3 真实 Uvicorn | 通过 | 08:00 自动生成 v2 候选并锁定 3 项执行事实；采用后推进到 08:04 生成 v3 候选；重启保留 awaiting revision `8`，拒绝后 paused revision `9`；8012 已释放 |
| 2026-08-01 | M4-4 SSE 专项测试 | 通过 | `31 passed`；覆盖共享流、节流、11 类消息契约、任务转换、256 条缓冲、精确补发、快照回退、OpenAPI、404 与 REST 轮询独立性 |
| 2026-08-01 | M4-4 后端回归 | 通过 | M4 组合 `62 passed in 2.39s`；后端全量 `129 passed in 4.98s`；`compileall`、`pip check` 和 `git diff --check` 通过 |
| 2026-08-01 | M4-4 真实 Uvicorn | 通过 | 8013 首次 snapshot；断线后连续补发 sequence `2-11`；旧流回退 snapshot；轮询为 v2/revision 4；重启后新 stream ID 且候选保留；端口和临时文件已清理 |
| 2026-08-02 | M4-5 前端回归 | 通过 | Vitest `17 passed`；类型检查与生产构建通过；覆盖会话复用、409、stream 去重、tick 和快照单调性 |
| 2026-08-02 | M4-5 后端回归 | 通过 | 后端全量 `129 passed in 4.50s`；`compileall`、`pip check` 和 `git diff --check` 通过 |
| 2026-08-02 | M4-5 浏览器动态闭环 | 通过 | 隔离双服务完成会话复用、开始/暂停/倍速/重置、自动事件、候选采用/保留、手动重规划、SSE→轮询→SSE 和 409 恢复 |
| 2026-08-02 | M4-5 响应式与离线 | 通过 | 1440×900 与 390×844 五页无文档级横向溢出，最小可见字号 14px；全 API 失败时明确离线只读、时间不会推进 |
| 2026-08-02 | M4-5A 有限事件与方案内容 | 通过 | 后端全量 `130 passed`、前端 `19 passed`、类型检查与生产构建通过；六事件逐一生成唯一候选，09:30 完成不回绕 |
| 2026-08-02 | M4-5B 方案依据完整展示 | 通过 | 前端 `19 passed`、类型检查与生产构建通过；1024/1280/1440/390 均无横向溢出，任务书和证据板块无内部滚动或裁切，控制台 0 error / 0 warning |
| 2026-08-02 | M4-5C Windows 一键启停 | 通过 | 旧错误状态可兼容识别；根目录停止后 4173/8000 释放，根目录启动后双服务健康并打开 Edge；重复启动 PID 不变 |

## 8. 问题、风险与处理方式

### 已知问题

1. 项目尚未创建 GitHub 远程仓库，原因是仓库名称尚未最终确认。
2. M3 场景、计划和业务审计仍为进程内存储；M4 运行会话、控制审计和确定性投影依据已进入 SQLite。比赛版重启可恢复运行会话，但通用场景/方案持久化仍未完成。
3. 真实机场数据接入需要数据授权、脱敏和接口适配，不作为比赛 MVP 的前置条件。
4. M4-5 已让在线五页消费 SQLite 运行会话、SSE 和 REST 恢复；M4-5A 已补齐六事件有限序列和完整方案任务书；M4-5B 已取消方案依据内部滚动并让中文方案摘要、任务书和权威证据完整展开；M4-5C 已恢复 Windows 一键启停和浏览器打开。M4-6 尚未完成长时间、性能、故障、多页面一致性和桌面/移动三轮连续演示，因此仍不能将整个 M4 标记完成。
5. M4-4 流缓冲只属于单个 Uvicorn 生命周期，不提供多 worker 或跨进程消息补发；服务重启、旧流或缓冲缺口会发送 SQLite 权威 snapshot，这符合单机比赛版边界。

### 已解决问题

1. 首轮测试发现唯一 ID 辅助函数的列表推导变量错误，导致合法场景触发 `NameError`；已改为显式传入 ID 字段，并用合法场景和重复资源测试验证。
2. 当前执行环境不能写入系统 pytest 临时目录；损坏 JSON 测试已改用仓库内固定夹具 `tests/fixtures/invalid-json.json`，避免环境依赖。
3. Windows 控制台初次输出中文摘要时编码不一致；验证脚本已显式使用 UTF-8 输出并复测通过。
4. OR-Tools 全局安装会升级 protobuf 并与现有 `mem0ai` 冲突；已撤销全局变更，改为项目 `.venv` 隔离安装。
5. 首轮前端生产构建受沙箱子进程权限限制；在获准环境中构建成功，代码本身无构建错误。
6. 首次浏览器加载出现 favicon 404；已添加 `frontend/public/favicon.svg`，复测控制台 0 错误。
7. FastAPI 0.140.7 / Starlette 1.3.1 对旧 `httpx` 测试客户端发出弃用警告；已按本地源码提示改用 `httpx2` 2.9.1，复测无警告。
8. 首版 500 异常日志会记录异常原文，可能包含本地路径；已改为只记录请求 ID 和异常类型，并添加响应与日志双重脱敏测试。

### 风险控制

- 不把外部 API 密钥写入代码或文档。
- 不在日志、截图、演示视频中出现可识别个人信息。
- 对 AI 解析结果执行 Pydantic 校验和硬约束校验；模型不可用时保留规则回退。
- 任何性能指标均标注为仿真测试目标，不外推为真实机场生产承诺。

## 9. 已完成阶段 M3 与下一步 M4

详细实施计划：[m3-plan.md](m3-plan.md)

1. [x] M3-0/1：冻结契约，创建 FastAPI 应用、健康检查、统一错误响应和最小测试。
2. [x] M3-2：实现线程安全内存仓库、不可变场景基线、版本快照和审计模型。
3. [x] M3-3：提供场景导入、列表和当前/历史版本查询接口。
4. [x] M3-4：提供事件应用、FIFO/CP-SAT 规划和不可变计划查询接口。
5. [x] M3-UI：完成五页标签式工作台、固定视口和任务/事件/资源跨页联动。
6. [x] M3-5：提供方案比较、审计查询和 `/api/v1/demo` 兼容输出。
7. [x] M3-6A：增加 API 优先、静态回退和统一错误的数据适配层。
8. [x] M3-6B：将五页工作台接入同一 API 数据源并显示来源状态。
9. [x] M3-7：升级一键脚本，同时管理前后端服务。
10. [x] M3-8：完成 API、五页界面、回退与启停的端到端审查。
11. [x] M3-9：完成 README、OpenAPI 说明、阶段 review、进度记录和提交。

M3 已关闭，M4-0 契约冻结、M4-1 运行会话、M4-2 确定性状态投影、M4-3 动态重规划、M4-4 实时流后端、M4-5 五页实时工作台、M4-5A 有限事件与方案内容修正、M4-5B 方案依据完整展示修正和 M4-5C Windows 一键启停修正均已完成。当前按 [M4 实时运行与动态重规划实施计划](m4-realtime-operations-plan.md) 进入 M4-6。未完成长时间、性能、故障与三轮连续演示验收前，不得将 M4 标记完成。

### M4 执行清单

详细契约：[m4-runtime-contract.md](m4-runtime-contract.md)

1. [x] M4-0：冻结运行会话、时钟、状态机、REST/SSE、revision、错误和恢复契约。
2. [x] M4-1：实现 SQLite 会话仓库、可注入时钟、创建/列表/查询及开始/暂停/倍速/重置。
3. [x] M4-2：实现任务、资源、航班和事件的确定性状态投影。
4. [x] M4-3：实现事件自动生效、执行事实冻结、滚动重规划和人工确认。
5. [x] M4-4：实现 SSE 推送、消息缓冲、断线重连和轮询恢复。
6. [x] M4-5：将五页工作台升级为实时运行界面。
7. [x] M4-5A：扩展六事件有限序列，公开当前/候选方案任务书和真实差异。
8. [x] M4-5B：取消方案依据内部滚动，完整展开中文方案摘要、任务书和权威证据。
9. [x] M4-5C：修复 Windows 一键启停的进程可执行路径记录和旧状态兼容校验。
10. [ ] M4-6：完成动态流程端到端、性能、故障和三轮连续演示验收。

下一唯一执行入口为 M4-6。M4-5A 至 M4-5C 保留 M4-5 的共享权威快照、实时控制、动态投影、候选人工决定、SSE 恢复和离线只读，并补齐后续事件、方案内容、完整展示及可靠启停；M4-6 只做端到端、性能、长时间、故障、多页面一致性和三轮连续演示总验收。

## 10. 新窗口恢复说明

新窗口接续本项目时，按以下顺序执行：

1. 阅读 `docs/project-progress.md` 全文，确认当前阶段、已知问题和下一步任务。
2. 阅读 `docs/project-plan.md`，确认需求边界和验收标准。
3. 执行 `git status --short --branch`，不要覆盖工作区已有改动。
4. 检查“已完成工作”末尾的最新日期记录和“验证记录”。
5. 只从“下一步任务”开始推进；完成后在本文档末尾追加新的日期记录。

## 11. 更新模板

后续每次完成一个工作单元，在“已完成工作”末尾追加：

```markdown
### YYYY-MM-DD / 阶段或任务名

- 完成：
- 关键文件：
- 验证：
- 已知问题/风险：
- 下一步：
```

不要删除历史记录；若决策发生变化，在“技术决策”表中更新当前结论，并在更新记录中说明变更原因。

## 12. 后续完成记录

### 2026-07-27 / Windows 一键启停脚本

- 完成：在项目根目录增加 `启动联保智调.cmd` 和 `停止联保智调.cmd`，支持双击刷新演示数据、启动 Vite、自动选择 `4173-4199` 空闲端口、优先用 Microsoft Edge 打开页面，以及按已记录的精确 PID 停止服务进程树。
- 关键文件：`启动联保智调.cmd`、`停止联保智调.cmd`、`scripts/start-project.ps1`、`scripts/stop-project.ps1`、`.gitignore`、`README.md`。
- 运行记录：PID、端口、进程启动时间和项目路径写入 `.runtime/server-state.json`；标准输出和错误日志保存在 `.runtime/`，该目录不进入 Git。重复启动只打开现有服务，不创建第二个 Vite 进程。
- 安全处理：停止前校验项目路径、PID、进程名和启动时间，避免 PID 重用导致误停；状态缺失时不扫描或结束未知 Node 进程；无效状态只清理状态文件。
- 验证：根目录启动入口成功在 `http://127.0.0.1:4173` 返回 HTTP 200，并创建 Microsoft Edge 进程；重复启动保持同一 PID；根目录停止入口成功结束对应进程并释放监听端口。PowerShell 语法检查、`19 passed`、`npm run typecheck`、`npm run build` 和 `git diff --check` 均通过。
- 已知边界：M2 尚无常驻 FastAPI 服务，因此当前脚本只启动 Vite 网站；M3 完成业务 API 后，需要扩展同一状态文件和停止逻辑以同时管理前后端进程。
- 下一步：按第 9 节进入 M3 FastAPI 业务接口与前后端联调。

### 2026-07-27 / M3 实施规划

- 完成：创建 `docs/m3-plan.md`，冻结 M3 的阶段边界、业务闭环、API 表、场景版本语义、事件防重复规则、统一错误、前端回退、启停升级、测试矩阵和退出条件。
- 核心决策：使用不可变场景基线和已应用事件集合重建新版本；事件应用和计划创建携带 `expected_version`；计划保存后不可修改；M3 使用进程内仓库，SQLite 后置。
- 执行顺序：先完成 FastAPI 骨架与错误契约，再实现仓库/版本、场景 API、事件与规划、比较与审计，最后进行前端联调和一键启停升级。
- 当前状态：仅完成规划，M3 功能代码尚未开始；正式执行入口为 `m3-plan.md` 的 M3-1。

### 2026-07-28 / M3-0 与 M3-1

- 完成：复核并冻结 M3 API 契约；创建 FastAPI 应用工厂、`/api/v1/health`、OpenAPI、请求 ID 中间件，以及 404、422、业务异常和 500 的统一错误响应。
- 关键文件：`backend/app/main.py`、`backend/app/api.py`、`backend/app/api_models.py`、`backend/app/errors.py`、`tests/test_api_health.py`、`backend/requirements.txt`。
- 验证：新增 6 项 API 测试；全量 `25 passed`；`pip check`、`compileall` 通过；真实 Uvicorn 下 health/OpenAPI 200、统一 404 通过，8000 端口已释放。
- 安全处理：公开 500 响应不包含异常原文，内部日志只记录请求 ID 与异常类型；请求数据校验详情不回显原始输入。
- 已解决问题：根据 Starlette 1.3.1 的迁移提示将测试客户端依赖由 `httpx` 调整为 `httpx2`，消除弃用警告。
- 已知边界：目前只有系统健康接口，尚无场景或规划业务接口；现有一键脚本仍只启动 Vite，这是 M3-1 的预期范围。
- 下一步：执行 M3-2，先实现内存仓库和版本/审计模型，不提前创建场景路由。

### 2026-07-28 / M3-2

- 完成：新增线程安全进程内场景仓库，将导入事件登记为待应用事件目录，并将当前基线保存为无待处理事件的不可变版本快照。
- 关键文件：`backend/app/repository.py`、`backend/app/audit_models.py`、`tests/test_repository.py`、`backend/app/__init__.py`。
- 一致性规则：所有存取使用防御性深拷贝；事件修订提交在 `RLock` 内校验 `expected_version`、事件注册/应用状态、版本号和完整事件序列，全部通过后才原子写入版本、事件状态和审计。
- 验证：新增 10 项仓库测试，覆盖外部修改隔离、新结构化事件、历史版本、冲突不写入、重复应用、无效修订、时区审计和双线程同版本竞争；全量 `35 passed`，`compileall` 和 `git diff --check` 通过。
- 设计调整：审计记录是领域持久化模型，不属于 HTTP 外壳，因此单独放入 `audit_models.py`，避免仓库依赖 `api_models.py`。
- 已知边界：当前仓库只存场景版本和审计；计划存储在 M3-4 接入。M3-2 未增加任何场景 HTTP 路由。
- 下一步：执行 M3-3，在仓库之上实现场景导入、摘要列表、当前/历史版本查询和统一异常映射。

### 2026-07-28 / M3-3

- 完成：新增场景应用服务和 `POST/GET /api/v1/scenarios`、`GET /api/v1/scenarios/{scenario_id}`，支持严格导入、分页摘要、当前/历史版本查询及统一 404/409 映射。
- 便利性：API 启动即加载内置仿真场景；用户无需先编写提示词或 JSON 即可查看 5 项任务、4 项资源和 2 个待处理事件的可规划示例。
- 可理解性：响应明确显示所选/当前版本、可用版本、进程内存储、安全提示、任务/资源/事件数量、可规划状态、数据缺口和确定性下一步建议。
- 实用性：资源类型不匹配时不会错误标记为可规划；非法引用保留匿名实体 ID 以便定位，同时不回显整份原始输入。
- 关键文件：`backend/app/services.py`、`backend/app/api_models.py`、`backend/app/api.py`、`backend/app/main.py`、`tests/test_api_scenarios.py`。
- 验证：新增 11 项场景 API 测试，全量 `46 passed`；`compileall` 通过；真实 Uvicorn 场景列表、详情和 OpenAPI 冒烟通过，8000 端口已释放。
- 契约细化：分页列表使用 `ScenarioListResponse` 返回 `items/total/offset/limit`，比裸数组更便于最终界面和用户判断。
- 已知边界：尚无事件应用和计划端点；内置场景当前通过 API 只读；前端仍读取静态演示 JSON。
- 下一步：执行 M3-4，完成结构化事件应用、FIFO/CP-SAT 计划创建和不可变计划查询，不接入自然语言 AI。

### 2026-07-28 / 可理解性 UX 检查点

- 完成：在继续 M3-4 前，重构演示页的信息层级为“这是做什么 → 三步怎么用 → 当前建议 → 具体安排 → 专业信息”，默认打开系统优化建议。
- 可理解性：将 FIFO、CP-SAT 和 P1/P2/P3 分别转换为原保障安排/航班变化后/系统优化建议及紧急/优先/常规；任务地点显示中文名称；风险、冲突和资源负载均使用行动语言解释。
- 功能保留：完整任务安排、资源负载、事件影响、独立约束检查和方案/版本信息仍可查看；算法与校验细节移入可展开的专业信息，未删除任何计算或证据链。
- 关键文件：`frontend/src/App.tsx`、`frontend/src/styles.css`、`docs/m3-ux-review.md`。
- 验证：`npm run typecheck` 通过；生产构建通过；桌面宽度无整体横向溢出；390×844 移动视图无整体横向溢出；方案切换、风险提示、推荐入口和专业信息展开均经浏览器实测通过。
- 安全边界：继续明确显示“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令”。
- 下一步：提交 UX 检查点后进入 M3-4；新 API 的计划标签、风险摘要和推荐动作保持与此页相同的普通语言语义，不接入自然语言 AI。

### 2026-07-28 / M3-4

- 完成：新增结构化事件应用、FIFO/CP-SAT 计划创建、场景计划筛选和完整计划查询接口；计划以防御性深拷贝保存并写入审计。
- 事件一致性：每个新版本从不可变基线重放已应用事件，分批应用不会重复累计延误；批次内事件原子提交，版本冲突、未知事件、重复事件和不一致登机口前态均不写状态。
- 计划一致性：计划计算前后都核对场景版本，保存前再次独立检查资源、容量、时间窗、路线和重复占用；同版本同算法不重复创建。
- 可理解性：响应把 FIFO/CP-SAT 分别显示为“原规则方案”和“系统优化建议”，提供结果摘要、取舍说明、推荐动作、计算依据和人工确认提示，不依赖自然语言 AI。
- 关键文件：`backend/app/api_models.py`、`backend/app/repository.py`、`backend/app/services.py`、`backend/app/api.py`、`tests/test_api_planning.py`、`tests/test_repository.py`、`docs/m3-04-review.md`。
- 验证：M3-4 API 工作流 13 项通过；计划仓库新增 2 项通过；全量 `61 passed`；`compileall` 通过；真实 Uvicorn 下完成 FIFO → 事件 → CP-SAT → 查询闭环，FIFO 4/5、优化 5/5、紧急任务 100%、冲突 0。
- 安全边界：接口继续返回教学仿真安全声明；未知航班等无效结构化事件返回安全业务错误，不回显内部异常或整份输入。
- 下一步：执行 M3-5，完成方案比较、公开审计查询和 `/api/v1/demo` 兼容输出。

### 2026-07-28 / M3-UI 标签式工作台

- 完成：将原有长页面和锚点导航重构为调度总览、任务计划、事件影响、资源态势、方案依据五个独立工作页；左侧按钮切换页面，桌面端不再依赖整页纵向滚动。
- 实用功能：增加全部/紧急/待协调任务筛选与详情联动、事件选择和字段影响分析、资源选择与负载评估、待命区域和可用时段、资源任务序列、区域覆盖、独立约束和计算链路复核。
- 用语调整：统一使用原始计划、规则重排、优化方案、待协调任务、独立约束复核等专业但可直读的表述，并为术语保留解释。
- 交互一致性：方案切换是全局状态；从任务进入资源、从资源返回任务或从总览进入事件/依据时，相关选择与当前方案保持一致。
- 关键文件：`frontend/src/App.tsx`、`frontend/src/styles.css`、`frontend/src/types.ts`、`docs/m3-tabbed-workspace-review.md`、`docs/m3-plan.md`。
- 验证：`npm run typecheck` 与生产构建通过；1440×900 和 390×844 下五页均无浏览器文档级横纵滚动；移动端使用顶部横向标签与当前页内部滚动；任务、事件、资源和跨页动作实测通过；控制台 0 error。
- 安全边界：安全声明在桌面和移动端均完整保留；所有指标继续来自后端确定性结果，前端未新增调度计算或 AI 决策。
- 后续重排：下一步仍为 M3-5；其后拆分为 M3-6A 数据适配、M3-6B 五页联调、M3-7 启停升级、M3-8 端到端审查和 M3-9 阶段收口，详见 `docs/m3-tabbed-workspace-review.md`。

### 2026-07-28 / M3-UI 字体可读性调整

- 完成：针对初版五页工作台字体偏小的问题建立统一大字号层，将辅助文字提升到不低于 14px、普通文字约 16px、主要标签约 18px、页面标题 30-32px、关键数字最高 36px。
- 布局补偿：同步扩大侧栏、页头、方案切换、表格行、状态标签和详情栏；移动端副标题改为完整换行，数据密集区域继续使用内部滚动，不恢复整页长页面模式。
- 可读性修正：方案说明允许换行；资源区域改为单列展示，避免放大后地点名称被截断；任务表保留内部横向滚动以容纳完整路径和服务时段。
- 验证：桌面 1440×900 与移动 390×844 五页实测的最小可见字号均为 14px；页面标题、活动标签、副标题和安全声明均未裁切；`body/html` 宽高继续等于视口；控制台 0 error。
- 安全与功能边界：未修改任何调度算法、指标或业务状态，只调整字体和响应式承载方式；M3-5 仍未开始。

### 2026-07-29 / M3-5

- 完成：新增 `POST /api/v1/scenarios/{scenario_id}/comparisons`、`GET /api/v1/scenarios/{scenario_id}/audit-records` 和 `GET /api/v1/demo`，后端现可独立完成 FIFO → 事件 → CP-SAT → 比较 → 审计闭环。
- 比较语义：只比较同一请求场景下两套已保存的不可变计划，允许比较该场景的不同版本计划；返回两份指标快照及“候选减基线”差值，固定按约束冲突、紧急任务保障率、任务完成率和等待时间生成结论，不依赖自然语言 AI。
- 审计语义：比较只追加 `comparison_created` 记录，不改变场景版本或计划；查询返回最近 `limit` 条记录并保持正序，只公开动作、版本、匿名实体 ID 和安全摘要，不包含原始请求、本地路径或个人信息。
- 演示兼容：`/api/v1/demo` 直接复用 `demo_export.py` 与确定性调度内核，响应逐字段等于 `frontend/public/demo-output.json`；事件后 FIFO 为 4/5、优化方案为 5/5、紧急任务保障率为 50%→100%、三视图违规数均为 0，安全声明保持完全一致。
- 关键文件：`backend/app/api_models.py`、`backend/app/repository.py`、`backend/app/services.py`、`backend/app/api.py`、`backend/app/demo_export.py`、`tests/test_api_workflow.py`、`docs/m3-05-review.md`。
- 验证：M3-5 专项 `6 passed`；全量 `67 passed`；`compileall` 和 `git diff --check` 通过；真实 Uvicorn 闭环全部成功，临时服务已关闭且 8015 端口已释放。
- 下一步：执行 M3-6A，建立单一 `loadDemoPayload()`，优先读取 `/api/v1/demo`，失败时回退静态 JSON，并覆盖 API 成功、静态回退、双失败和重试恢复；暂不改动五页工作台业务结构。

### 2026-07-29 / M3-6A

- 完成：新增单一 `loadDemoPayload()`，按 `/api/v1/demo` → `demo-output.json` 顺序加载，分别返回 `source: "api"` 和 `source: "static_fallback"`；`App.tsx` 已删除直接静态 `fetch`，刷新和错误页重试均重新执行完整加载链。
- 错误与契约：网络错误、非 2xx、非法 JSON 和基础契约缺失统一归类；双失败时页面只显示可操作的安全提示，内部失败摘要不保留原始响应、异常文本或本地路径。加载器仅校验数据外形，不在前端复制调度与比较公式。
- 本地连接：Vite 增加 `/api` 代理，默认目标为 `http://127.0.0.1:8000`，支持 `VITE_API_TARGET` 覆盖，不增加通配 CORS。
- 关键文件：`frontend/src/api.ts`、`frontend/src/api.test.ts`、`frontend/src/types.ts`、`frontend/src/App.tsx`、`frontend/vite.config.ts`、`frontend/package.json`、`docs/m3-06a-review.md`。
- 验证：前端 `4 passed`、类型检查和生产构建通过；后端全量 `67 passed`，`compileall` 与 `git diff --check` 通过。真实服务下 Vite 代理访问 API 返回 200；后端停止后代理返回 502、静态文件返回 200，三套视图和优化 5/5 均保持；临时服务已停止，8000/4188 端口已释放。
- 安全与边界：完整保留“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令”；本阶段未增加来源标签、五页业务改造、自然语言 AI、持久化或真实机场生产数据。
- 下一步：执行 M3-6B，在页面保存并显示 `source`，验证 API 数据、静态回退和重试后五页切换及跨页状态一致性，不新增第二套加载逻辑。

### 2026-07-29 / M3-6B

- 完成：保存 `loadDemoPayload()` 返回的数据来源；五页共享页头分别显示“服务数据”或“本地演示数据”，侧栏同步显示真实连接状态，静态回退不再误报服务正常。
- 状态一致性：新增 `resolveSelectedId()` 及 3 项单元测试；任务、事件和资源选择只在新载荷就绪后协调，避免刷新中间态清空仍然有效的选择。
- 真实恢复闭环：以资源页、原始计划、紧急筛选、`TASK-005`、第二项 `SIM218` 登机口事件和 `BUS-01` 为基准，完成 API → 静态回退 → API 恢复，两次应用内刷新后全部状态保持。
- 浏览器验证：API 在线五页均显示服务数据；1440×900 与 390×844 的文档宽高等于视口，来源标签、安全声明和页面正文无重叠；控制台仅有后端停止期间一条预期 502。
- 关键文件：`frontend/src/App.tsx`、`frontend/src/styles.css`、`frontend/src/selection.ts`、`frontend/src/selection.test.ts`、`docs/m3-06b-review.md`。
- 自动化验证：前端 `7 passed`、类型检查和生产构建通过；后端 `67 passed`；`compileall` 与 `git diff --check` 通过；临时浏览器和服务已关闭，`8000`、`4189` 端口已释放。
- 安全与边界：完整保留“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令”；未增加第二套加载逻辑、前端调度计算、自然语言 AI、持久化或真实机场生产数据。
- 下一步：执行 M3-7，升级一键启停脚本，等待后端健康后启动前端，并通过状态文件精确管理和停止双服务。

### 2026-07-30 / M3-7

- 完成：将一键启动从单 Vite 进程升级为 FastAPI + Vite 服务组；后端健康后才启动前端，前端代理使用实际后端端口，全部就绪后优先打开 Microsoft Edge。
- 状态模型：`.runtime/server-state.json` 升级为 `schema_version: 2` 和 `services[]`，分别记录前后端 PID、进程名、启动时间、可执行文件路径、端口、URL、工作目录与日志。
- 精确停止：停止前统一验证项目路径、服务名、PID、进程名、启动时间和可读取的可执行文件路径，再按“前端 → 后端”停止；无状态重复停止幂等，异常或不匹配状态不结束任何进程。
- 失败与兼容：启动任一步失败会回收本次创建的所有服务；旧版单前端状态仍可安全停止，运行中的旧版或部分服务不会被新启动流程覆盖。
- 关键文件：`scripts/start-project.ps1`、`scripts/stop-project.ps1`、`docs/m3-07-review.md`、`README.md`、`docs/m3-plan.md`、`docs/m3-tabbed-workspace-review.md`。
- 验证：双服务、健康等待、代理 200、重复启动 PID 复用、Edge 打开、根目录停止入口、无关 Node/Python 存活、端口释放均通过；前端 `7 passed`、后端 `67 passed`、类型检查、生产构建、`compileall`、PowerShell 语法和 `git diff --check` 通过。
- 安全与边界：完整保留“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令”；Edge 不属于服务组，停止项目不会关闭浏览器。
- 下一步：执行 M3-8，联合审查 API 全流程、五页桌面/移动端、静态回退与恢复、双服务启停和最终安全文案。

### 2026-07-30 / M3-8

- 完成：使用独立 Uvicorn `8019` 走通 health → FIFO → 两项扰动 → CP-SAT → 比较 → 方案回查/列表 → 审计时间线的真实 HTTP 闭环，比较操作不修改场景或已保存方案。
- 指标证据：FIFO 完成 `4/5`、紧急任务 `50%`；CP-SAT 完成 `5/5`、紧急任务 `100%`；已分配 `+1`、待协调 `-1`、紧急完成率 `+50` 个百分点，两个方案硬约束违规均为 `0`。
- 浏览器证据：Microsoft Edge 覆盖 1440×900 和 390×844 的调度总览、任务计划、事件影响、资源态势、方案依据；任一时刻只有一个工作页，最小可见字号 `14px`，无文档级溢出、控件溢出、在线控制台错误或页面异常。
- 交互与恢复：紧急筛选、`TASK-005`、原始计划、第二项登机口事件和 `BUS-01` 通过；服务数据 → 本地演示数据 → 服务数据、API/静态双失败和重试恢复均保持页面与方案状态。
- 双服务与隔离：运行状态保持 `ready`，后端直连和 Vite 代理健康接口一致；临时 `8019` 停止后端口释放，原 `8000/4173` 服务未受影响。
- 全量回归：后端 `67 passed`、前端 `7 passed`、类型检查、生产构建、`compileall`、`pip check`、PowerShell 语法和 `git diff --check` 通过；浏览器与 API 证据保存在忽略提交的 `output/playwright/m3-8/`。
- 关键文件：`docs/m3-08-review.md`、`docs/project-progress.md`、`docs/m3-plan.md`、`docs/m3-tabbed-workspace-review.md`、`README.md`。
- 安全与边界：完整保留“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令。”；本阶段未增加真实机场数据、自然语言 AI、持久化、前端调度计算或生产控制能力。
- 下一步：执行 M3-9，只做 README/OpenAPI 说明复核、M3 总复盘、退出条件确认和阶段收口提交，不扩张界面或业务功能。

### 2026-07-30 / M4 实时运行需求冻结

- 现状确认：M3-8 虽已由 FastAPI 提供数据，但前端仅在首次加载或手动刷新时读取 `/api/v1/demo`，正常界面仍是三套预生成方案快照，没有自动推进的仿真时钟、任务/资源状态转换或服务器推送。
- 完成：新增 `docs/m4-realtime-operations-plan.md`，将 M4 冻结为后端权威仿真时钟、运行控制、任务/资源/航班状态机、定时事件自动生效、滚动重规划、人工确认、SSE 推送、轮询恢复和 SQLite 关键状态持久化七个执行单元。
- 关键决策：实时指可复现的仿真实时运行，不冒充真实机场生产数据；CP-SAT 只在事件或关键边界触发；事件触发时仿真自动暂停；已完成和正在执行任务不得被重规划改写；候选方案必须人工确认后才能成为当前执行方案。
- 验收收紧：静态 JSON 只保留为明确标识的离线只读降级。只做定时刷新、随机数字变化或固定快照切换，均不视为完成 M4。
- 文档同步：更新总体计划、最终展示蓝图、README、技术决策、已知问题和下一阶段入口，并保留完整安全声明。
- 下一步：先完成 M3-9 阶段收口，随后进入 M4-0 冻结具体数据模型、REST/SSE 契约、时钟边界和自动化测试样例。

### 2026-07-30 / M3-9 阶段收口

- 完成：新增 `docs/m3-openapi-guide.md` 和 `docs/m3-review.md`，补齐 Swagger 完整业务闭环、请求体、冲突恢复、PowerShell 示例、阶段成果、退出条件和已知边界。
- OpenAPI：应用说明改为明确的“场景 → FIFO → 事件 → CP-SAT → 比较 → 审计”顺序，加入五组业务标签和完整安全声明；健康检查摘要统一为中文，并以自动化测试锁定元数据。
- 文档同步：README、M3 计划和持续交接入口均将 M3-9 标为完成，当前阶段切换为 M3 已关闭，唯一下一入口为 M4-0。
- 验证：后端 `67 passed`、前端 `7 passed`、类型检查、生产构建、`compileall`、`pip check`、OpenAPI 元数据、PowerShell 语法与 `git diff --check` 全部通过。
- Reader Test：独立读者能够仅根据 M3 总复盘和 OpenAPI 指南说明当前能力、复现 API 闭环、处理 409 冲突、区分 `/demo` 与业务接口，并准确指出 M4 尚未实现。
- 已知边界：M3 仍使用进程内仓库和一次性演示快照；GitHub 远程仓库仍待名称确认。这些边界未被收口文档包装成已完成功能。
- 下一步：执行 M4-0，冻结运行会话、仿真时钟、状态机、REST/SSE、修订冲突和测试样例，不再新增 M3 子任务。

### 2026-07-30 / M4-0 实时运行契约冻结

- 完成：新增 `backend/app/runtime_models.py` 与 `backend/app/runtime_stream_models.py`，冻结运行会话、时钟、任务/资源/航班/事件投影、控制请求、会话恢复摘要和 11 类 SSE payload；所有模型继承严格字段策略。
- 契约：新增 `docs/m4-runtime-contract.md`，明确左闭右开时间边界、事件准确时刻自动暂停、用户/自动状态转换、11 条 REST 路径、错误码、SQLite 恢复和后续单元职责。
- 关键修正：将持久业务 `revision` 与 SSE `stream_id + sequence` 分离；tick/heartbeat 不增加 revision。增加分页会话列表，保证重启后能找回 SQLite 会话。
- 一致性：候选方案只在等待确认状态存在；affected 任务必须保留当前 assignment/resource/event；同一事件批次版本只增加 1；可执行候选违规数固定为 0。
- Review：发现并修复安全声明覆盖、事件字段状态范围、会话恢复入口和 M3 静态计划去重不适用于运行时滚动候选等边界，详见 `docs/m4-00-review.md`。
- 验证：M4-0 专项 `14 passed`；后端全量 `81 passed`；`compileall`、`pip check` 和 `git diff --check` 通过。
- 安全与现状：完整保留教学仿真安全声明；本单元没有实现后台时钟、SQLite、运行路由、自动重规划、SSE 连接或前端动态变化，当前页面仍是 M3 快照。
- 下一步：执行 M4-1，实现 SQLite 会话仓库、可注入时钟、创建/列表/详情以及开始/暂停/倍速/重置，不提前进入 M4-2/M4-4。

### 2026-07-31 / M4-1 运行会话、权威时钟与 SQLite

- 完成：新增 `backend/app/runtime_repository.py` 和 `backend/app/runtime_services.py`，实现 SQLite 会话/控制审计、可注入墙钟与单调时钟、会话创建/分页找回/详情，以及开始、暂停、继续、倍速和确认重置状态机。
- 时间语义：运行快照按“持久仿真时间锚点 + 单调经过时间 × 倍速”计算；暂停不漂移，倍速切换不跳时，墙钟回拨不导致仿真倒退；到达场景窗口终点只持久化一次完成边界。
- 并发与恢复：所有控制写入使用 `expected_revision` CAS，状态和审计同事务提交；两个独立 SQLite 连接并发更新只有一个成功。服务重启把 `running/replanning` 安全恢复为 `paused` 并增加 revision，分页列表可重新发现会话。
- API：公开 `runtime` OpenAPI 标签及创建、列表、详情、开始、暂停、倍速、重置七项操作；统一映射会话/方案缺失、场景/版本不匹配、硬约束违规、陈旧 revision、非法状态和持久化错误。`replan`、候选采用/拒绝和 SSE 未提前暴露。
- 复核修正：假时钟发现墙钟回拨会使 `updated_at` 早于创建时间，现已保证业务更新时间非递减；列表状态筛选会先结算已到窗口终点的运行会话；非法时间戳在开启事务前拒绝，避免连接残留未提交事务。
- 验证：后端全量 `95 passed`，`compileall`、`pip check`、`git diff --check` 通过；真实 Uvicorn 完成创建 → 开始 → 时间推进 → 暂停，并在强制重启后从 SQLite 找回同一会话，状态 `running` → `paused`、revision `4` → `5`。
- 安全与边界：所有运行响应保留完整教学仿真安全声明。M4-1 快照中的任务/资源/航班/事件仍为空数组，前端仍是 M3 静态快照；M4-2 才实现确定性状态投影，M4-3/M4-4 才实现动态重规划与 SSE。
- 详细审查：见 `docs/m4-01-review.md`。
- 下一步：执行 M4-2，实现任务、资源、航班和事件在所有左闭右开时间边界上的确定性状态投影，不提前实现事件自动重规划或前端平行状态机。

### 2026-07-31 / M4-2 确定性运行状态投影

- 完成：新增 `backend/app/runtime_projection.py`，由持久化场景版本、当前确认方案、完整事件目录和后端权威仿真时间纯计算任务、资源、航班、事件及下一状态边界；`ready` 不提前显示作业已开始，运行后统一使用左闭右开边界。
- 持久化与兼容：`runtime_sessions` 新增可空 `projection_source_json` 并自动迁移 M4-1 数据库。新会话保存完整投影依据；旧会话能从 M3 仓储恢复时无 revision、无控制审计补写，不能恢复时保留空投影而不编造数据。
- 状态证据：零分钟移动直接进入等待或服务；资源只展示区域间进度，不生成坐标或真实车辆轨迹；未应用事件到时只变为 `triggered`，已包含在场景版本中的事件用审计 `version_after` 显示为 `resolved`。
- 验证：M4-2 专项 `26 passed`，后端全量 `107 passed`，`compileall`、`pip check`、`git diff --check` 通过。真实 Uvicorn 在 `15x` 下从 `08:00` 推进到 `08:13:39`，任务、资源、航班和事件均变化，revision 保持 `2`、审计保持 2 条。
- 重启：真实进程停止并用同一临时 SQLite 重启后，会话从 `running` revision `2` 恢复为 `paused` revision `3`，审计新增且仅新增 `service_restarted`；新进程没有原方案对象仍返回 5 任务、4 资源、2 航班和 2 事件。验收进程已精确停止，8011 端口已释放。
- 安全与边界：所有运行响应继续固定包含“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令。”。M4-2 不应用事件、不重规划、不生成候选方案、不实现 SSE，也不改变仍为 M3 快照的前端。
- 详细计划与审查：见 `docs/m4-02-plan.md`、`docs/m4-02-review.md`。
- 下一步：执行 M4-3，实现事件准确时刻的原子应用、执行事实冻结、滚动重规划和候选方案人工采用/拒绝，不提前进入 SSE 或前端平行状态机。

### 2026-08-01 / M4-3 事件落地、滚动重规划与人工确认

- 完成：新增 `backend/app/runtime_planning.py`，把事件准确边界应用、执行事实冻结、剩余任务滚动 CP-SAT 和完整方案独立硬约束复核分离为可测试的确定性步骤；`RuntimeProjectionSource` 扩展为可跨版本和跨重启恢复的运行聚合。
- 事件事务：运行查询或控制到达最早未应用事件时，只推进到该准确时刻；相同 `occurred_at` 的事件按 ID 稳定排序并合并为一个批次、一个新场景版本和一次自动重规划。并发物化通过 revision CAS 保证只有一个写入成功。
- 执行保护：边界时刻已经完成、移动中、现场等待或保障中的任务保留原 assignment ID、资源、路线和四个时间边界；事件只更新未冻结任务。候选方案在服务边界再次调用 `validate_plan()`，硬约束不为零时不得进入待确认状态。
- 人工闭环：新增手动重规划、候选采用和候选拒绝三条 REST 路由。所有决定同时校验 expected revision 与 candidate plan ID；采用后切换 active plan，拒绝后保留原方案，二者都保留已发生的事件和场景版本事实。
- 失败与恢复：普通求解失败保留 active plan 并回到 `paused`；运行事实不一致进入不可恢复 `failed`；SQLite 写入失败拒绝部分写入并返回统一错误。`awaiting_confirmation` 跨重启保留候选，意外中断的 `replanning` 安全恢复为 `paused` 后允许人工重算。
- 真实服务验收：Uvicorn `8012` 中，08:00 事件生成场景 v2、revision `4` 候选并锁定 3 项执行事实；采用后推进到 08:04 生成场景 v3、revision `8` 候选；重启后仍为 `awaiting_confirmation`，拒绝后为 `paused` revision `9`。验收数据库、临时目录和相关进程已清除，8012 已释放。
- 最终验证：M4-3 专项 `10 passed`，M4 组合运行测试（含 API）`51 passed in 2.26s`，后端全量 `118 passed in 4.41s`；`compileall`、`pip check`（No broken requirements found）和 `git diff --check` 通过。
- 安全与边界：所有运行响应继续固定包含“仅供教学仿真与辅助决策使用，不构成真实机场运行、放行、登机、改签或车辆控制指令。”。本单元没有实现 SSE、消息缓冲、浏览器轮询恢复或前端运行控制，当前网站仍是 M3 快照。
- 详细计划与审查：见 `docs/m4-03-plan.md`、`docs/m4-03-review.md`。
- 下一步：执行 M4-4，建立 SSE 流、消息缓冲、`Last-Event-ID` 重连和短轮询回退；继续复用后端权威快照、revision 和现有运行状态，不创建平行时钟或第二套业务状态。

### 2026-08-01 / M4-4 SSE 实时流、消息缓冲与断线恢复

- 完成：新增 `backend/app/runtime_stream.py`，为每个运行会话维护单一进程内 stream ID、严格递增 sequence、256 条环形缓冲、最近权威快照和审计游标；多个连接共享同一序列，重复采样不会重复发布同一业务审计。
- 消息：SQLite 控制审计提供业务 revision，前后权威快照提供任务与投影差异；系统可发布 snapshot、tick、task transition、事件应用、自动/手动重规划、候选就绪、失败、采用/拒绝、完成和 heartbeat，tick/heartbeat 不写数据库。
- HTTP：公开 `GET /api/v1/runtime-sessions/{session_id}/stream`，标准帧包含 `id/event/data`；响应禁止缓存和代理缓冲。会话不存在时在建立流前继续返回统一 JSON `404 runtime_session_not_found`，流建立后的服务错误安全结束连接。
- 恢复：当前流且 sequence 仍连续时按 `Last-Event-ID` 精确补发；非法 ID、旧流、超前 ID、缓冲淘汰或服务重启时发送完整权威 snapshot。现有 GET 快照保持独立，可由 M4-5 直接用于两秒短轮询。
- 真实 Uvicorn：8013 首次连接返回完整 snapshot；会话在断线期间推进到场景 v2/revision `4` 后，携带 sequence 1 重连连续补发 sequence `2-11`，包含事件、重规划、候选、三项任务转换、snapshot 和 heartbeat；旧 stream ID 只返回当前 snapshot。
- 重启：精确停止旧监听 PID 后，用同一专用 SQLite 启动新进程；流从 `STREAM-834B595FD8724AE8` 切换为 `STREAM-D35CBCC6FAC948E0:1`，会话仍为 `awaiting_confirmation`、revision `4`、场景 v2，候选 ID 保持不变。
- 验证：M4-4 专项 `31 passed`，M4 组合 `62 passed in 2.39s`，后端全量 `129 passed in 4.98s`；`compileall`、`pip check`（No broken requirements found）和 `git diff --check` 通过。
- 清理：8013 已释放，无 M4-4 验收 Python 进程；17 个仅用于本次验收的 SQLite/WAL/SHM、SSE 和 curl 错误捕获文件已删除，不涉及项目数据。
- 安全与边界：所有 snapshot 继续携带完整教学仿真安全声明。stream、sequence、tick、heartbeat 和缓冲不持久化，不支持多 worker 或跨进程补发；前端仍是 M3 快照，尚未显示实时连接或运行控制。
- 详细计划与审查：见 `docs/m4-04-plan.md`、`docs/m4-04-review.md`。
- 下一步：执行 M4-5，把运行会话创建/找回、控制条、连接状态、SSE 消费、两秒轮询、任务/资源/航班/事件动态状态和候选采用/拒绝接入五页工作台；不得在前端重算仿真时间、任务状态或方案。

### 2026-08-02 / M4-5 五页实时运行工作台

- 完成：正常在线流程基于合成场景的确定性 CP-SAT 初始方案查找最近匹配的 SQLite 会话，无匹配才创建；刷新前后会话 ID 保持 `RUN-6F1B784769204E3C`，会话总数始终为 1。
- 共享状态：新增 `RuntimeWorkspace` 和 `useRuntimeSession`，五页共享同一 `RuntimeSessionSnapshot`、连接状态与任务/资源/事件选择；前端不推进时钟、不推导业务投影、不应用事件或运行规划器。
- 实时恢复：消费 11 类 SSE 消息，snapshot 原子替换、tick 只更新后端 clock，按 `stream_id + sequence` 去重；连续失败后每 2 秒读取 REST 快照，恢复流后停止轮询并忽略已在途的旧轮询响应。
- 控制并发：开始、暂停、`1x/5x/15x`、确认式重置、手动重规划和候选采用/保留均携带 revision；候选决定另带候选 ID；409 后刷新权威快照且不重放旧命令，快速重复点击由同步 busy 引用拦截。
- 五页动态：总览显示当前事实/系统动作/用户决定；任务、事件、航班和资源展示后端实时投影；方案页只展示可复核的方案 ID、受影响任务和后端约束语义，候选采用前不替换 active plan，也不伪造尚未公开的候选指标。
- 离线与非循环：运行 API 不可用时回到五页静态快照并明确显示“离线只读”“时间不会推进”，控制不可用；在线时间只按后端边界单向推进到完成，不循环播放或随机制造状态变化。
- 真实浏览器：隔离后端 8001、前端 4174 和专用 SQLite 完成开始/暂停/倍速/重置、08:00 自动事件、候选保留与采用、手动重规划、SSE→轮询→SSE、409 和全 API 离线分支；1440×900 与 390×844 五页无文档级横向溢出，最小可见字号 14px，安全声明完整。
- 验证：前端 `17 passed`、类型检查和生产构建通过；后端全量 `129 passed in 4.50s`，`compileall`、`pip check` 和 `git diff --check` 通过；正式 8000/4173 服务正常在线控制台为 0 error / 0 warning。
- 安全与边界：只使用合成教学数据，不接真实机场生产数据或控制系统；不实现 AI 事件解析、随机场景生成、多 worker、分布式 SSE 或 PostgreSQL；M4-6 的长时、性能、故障和三轮连续演示尚未完成。
- 详细计划与审查：见 `docs/m4-05-plan.md`、`docs/m4-05-review.md`。
- 下一步：只进入 M4-6，完成端到端、性能、长时间、故障、多页面一致性和桌面/移动三轮连续演示验收；不得借验收扩张到真实生产接入、AI 解析或随机场景生成。

### 2026-08-02 / M4-5A 有限事件序列与方案内容可读性修正

- 完成：将教学场景由 2 条事件、5 项任务扩展为 6 条固定一次性事件、10 项匿名任务、3 个虚构航班和 5 项资源；事件分布在 08:00、08:04、08:16、08:28、08:40、08:52，运行窗口到 09:30 正常完成，不循环、不随机。
- 状态所有权：后端时钟、事件目录、当前计划、候选计划、任务投影和 CP-SAT 继续是唯一业务事实；前端没有增加第二时钟、事件生成器、状态机或规划器。每条事件仍原子应用一次，候选硬约束违规为 0，采用前冻结等待人工确认。
- 方案可读性：运行快照公开与顶层 ID 一致的当前/候选完整计划；方案页显示中文用途、已安排数、平均等待、资源利用率、关键任务完成率、硬约束、逐任务航班/资源/时段/路线、未分配原因和真实差异。旧 SQLite 投影依据不可恢复时保持明细为空，不补造数据。
- 会话兼容：会话恢复同时核对完整任务 ID 和事件 ID 集合，旧 2 事件会话不会被新 6 事件场景误复用。事件投影直接提供类型、航班、中文事实说明和备注，不依赖初始静态 JSON 拼接说明。
- 浏览器证据：隔离会话 `RUN-F41B9EB5748A4B62` 在 15x 下依次生成 V2/R3、V3/R8、V4/R12、V5/R16、V6/R20、V7/R24 六个不同候选；全部人工采用后于 09:30 进入 `completed`，V7 不回绕。完成页显示 6 条事件全部“处理完成”，当前计划 10/10、硬约束 0。
- 响应式与控制台：1024×768、1280×720、1440×900 和 390×844 的事件/方案页均无文档级横向溢出或任务文本裁切；移动导航只在自身容器横向滚动；正常页面控制台 0 error / 0 warning。验收截图位于忽略提交的 `output/playwright/m4-5a/`。
- 验证：后端全量 `130 passed`，前端 `19 passed`，`npm run typecheck`、`npm run build`、`compileall`、`pip check` 和 `git diff --check` 全部通过；生产构建 CSS 53.74 kB、JS 280.53 kB。
- 安全与边界：仍只使用可复现合成教学数据，不接真实机场生产数据、旅客信息或控制系统；不实现无限事件、循环播放、前端随机业务变化或 AI 直接排班。AI 后续只能增强事件解析、追问和解释，不能绕过 CP-SAT、硬约束复核和人工确认。
- 详细计划与审查：见 `docs/m4-05a-plan.md`、`docs/m4-05a-review.md`。
- 下一步：M4-6 仍是唯一入口，只完成端到端、性能、长时间、故障、多页面一致性和三轮连续演示总验收，不得将 M4-5A 算作 M4-6 或提前宣称整个 M4 完成。

### 2026-08-02 / M4-5B 方案依据完整展示修正

- 根因：正式后端仍是 M4-5A 提交前启动的旧进程，运行快照只有方案 ID；同时方案依据使用固定三行网格和多层内部滚动，导致当前/候选状态与权威证据被压缩，任务书区域又出现大片空白。
- 服务恢复：使用项目脚本安全重启正式 8000/4173 服务后，OpenAPI 已包含 `active_plan_detail` 与 `candidate_plan_detail`，新会话加载 10 项任务和 6 条事件；前端不根据 ID 补造方案，明细缺失时明确提示同步问题。
- 方案摘要：当前与候选卡直接显示中文算法、已安排任务数、关键任务完成率和硬约束数；`PLAN-...` 只保留为次级审计编号。下方任务书继续展示逐项任务、虚构航班、资源、服务时段、路线和真实差异，不依赖未来 AI。
- 布局：方案依据改为自然高度纵向布局，取消候选区、任务列表、权威证据和运行链路的内部滚动；权威证据按宽屏三列、中屏两列、手机单列完整展开，移动端运行链路改为单列。
- 浏览器证据：正式会话 `RUN-04016FC0217B4AF7` 在 V2/R5 待确认态显示当前 9/10、候选 10/10、关键任务 100%、硬约束 0；当前与候选共 20 条任务全部展开，候选有 2 项真实调整、8 项保持不变。1024、1280、1440、390 宽度均无横向溢出，任务列表和证据板块 `clientHeight = scrollHeight`，控制台 0 error / 0 warning。
- 验证：前端 `19 passed`、`npm run typecheck`、`npm run build` 和 `git diff --check` 通过；生产构建 CSS 55.11 kB、JS 281.89 kB。本轮未改后端代码，后端全量回归沿用 M4-5A 的 `130 passed` 基线。
- 安全与边界：后端权威状态、CP-SAT、硬约束检查、人工确认、SSE 和离线只读均未改变；AI 后续只能增强理解与解释，不能替代确定性规划或人工决定。M4-6 尚未开始。
- 详细审查：见 `docs/m4-05b-review.md`。
- 下一步：M4-6 仍是唯一入口，只完成端到端、性能、长时间、故障、多页面一致性和三轮连续演示总验收。

### 2026-08-02 / M4-5C Windows 一键启停进程身份修正

- 根因：Windows 在前端进程刚启动时将 `System.Diagnostics.Process.Path` 误读为 `C:\Windows\System32\ntdll.dll`，状态文件因此与实际 `E:\nodejs\node.exe` 不一致；后续启动和停止均被严格身份校验安全拒绝，启动入口无法执行打开浏览器步骤。
- 修复：服务记录改为显式保存启动时使用的 `.venv` Python 或 Node 可执行路径；已有状态若保存非 `.exe` 路径，只回退到项目 Python 或当前 Node 可信路径，其他真正不匹配仍拒绝操作。
- 安全校验：项目根目录、服务名称、PID、进程名、启动时间和实际可执行路径继续全部核对；未降低对未知记录、PID 复用或其他进程的保护。
- 真实停止：根目录停止入口精确结束前端 PID `3716`、后端 PID `27772`，状态文件删除且 4173/8000 均释放，未停止浏览器或其他 Node/Python 进程。
- 真实启动：根目录启动入口重新生成演示数据，后端在 8000 健康、前端在 4173 返回 HTTP 200，并成功打开 Microsoft Edge；新状态正确记录 `E:\ican\.venv\Scripts\python.exe` 与 `E:\nodejs\node.exe`。
- 重复启动：再次执行时后端 PID `16232`、前端 PID `23388` 均保持不变，确认复用同一健康服务组且不会重复创建进程。
- 验证：全部 PowerShell 脚本语法解析通过，真实启停闭环通过，`git diff --check` 通过；正式服务保持运行。
- 安全与边界：本轮不改运行时业务状态、CP-SAT、硬约束检查、SSE、人工确认或教学仿真声明；M4-6 尚未开始。
- 详细审查：见 `docs/m4-05c-review.md`。
- 下一步：M4-6 仍是唯一入口，只完成端到端、性能、长时间、故障、多页面一致性和三轮连续演示总验收。

### 2026-08-04 / M4-6 实时系统总验收

- 完成：新增独立 M4 REST 总验收，使用隔离 SQLite 和假时钟连续执行全采用、采用/保留交替、重置后再运行三轮六事件流程；18 个候选全部唯一，候选和最终当前方案硬约束违规为 0，每轮均到达场景 v7、09:30 `completed` 且不回绕。
- 缺陷修复：验收发现采用事件 1、拒绝事件 2 后，部分任务按旧当前方案开始并冻结，已更新场景路线与冻结分配不一致，事件 3 会因 `task_route` 违规安全暂停。现以当前执行分配作为已开始任务的权威路线事实，再应用后续事件；拒绝候选不被静默采用，事件/航班事实不回滚，未来任务继续使用最新场景求解和独立复核。
- 自动验证：拒绝后继续与 M4-6 专项 `4 passed in 1.48s`；后端全量 `133 passed in 5.87s`；前端 `19 passed`；`compileall`、`pip check`、类型检查、生产构建、PowerShell 语法和 `git diff --check` 通过。构建产物 CSS 55.11 kB、JS 281.89 kB。
- 性能：本机小型合成场景初始 CP-SAT 65.855 ms；六次滚动重排最大 24.746 ms；100 次快照平均 2.012 ms、最大 4.371 ms；完整六事件隔离轮次 236.636 ms。该结果不外推为真实机场生产承诺。
- 浏览器三轮：桌面全采用会话 `RUN-9B89ECD3212542DC`、移动采用/保留交替会话 `RUN-513DB162C36C45F1`、双页面重启恢复会话 `RUN-2639C8D8CCC848A6` 均生成六个唯一候选并完成至 09:30；正常流程控制台 0 error / 0 warning，桌面/移动无文档级横向溢出，方案列表无内部纵向滚动。
- 恢复与并发：主动中断 SSE 后页面进入“快照恢复”，连续 6 次按 2 秒读取 REST，流恢复后停止轮询；等待确认状态重启后候选原样保留。双页并发采用同一候选时后端记录一次 200、一次预期 409，两个页面收敛到同一 revision 和当前方案。
- 启动与日志：PowerShell 脚本语法通过；再次执行 `start-project.ps1 -NoBrowser` 正确复用后端 PID 4708、前端 PID 3200；后端健康、前端 HTTP 200，正式日志无 5xx、Traceback 或异常退出。
- M4 结论：`docs/m4-realtime-operations-plan.md` 第 12 节九项硬性退出条件全部通过，M4-0 至 M4-6 已完成。截图证据位于忽略提交的 `output/playwright/m4-6/`，详细计划与审查见 `docs/m4-06-plan.md`、`docs/m4-06-review.md`。
- 安全与边界：继续使用 6 条固定、有限、一次性的匿名合成事件，不随机、不循环；不接 AI、真实机场生产数据、个人信息、多 worker、PostgreSQL 或前端第二状态机。完整安全声明保持不变。
- 下一步：M5 是唯一入口，只实现结构化事件解析、字段缺失追问、调度目标选择、方案解释和无模型回退；AI 不得绕过 Pydantic、CP-SAT、独立硬约束校验、revision 并发保护或人工确认。

### 2026-08-05 / M5-0 AI 辅助契约

- 完成：新增 M5-0 严格契约，冻结自然语言事件草稿、逐字段依据、缺失追问、有限目标建议、场景/运行方案解释、来源透明度和无模型回退语义；本单元不公开路由、不调用模型、不读取密钥、不修改前端或 M4 运行状态。
- 状态所有权：请求必须绑定场景 `expected_version` 或运行 `expected_revision`；草稿固定 `requires_human_confirmation=true`、`applies_automatically=false`，解释固定 `modifies_plan=false`；模型不能推进时间、应用事件、生成自由排班或采用候选。
- 草稿契约：完整延误或登机口变更必须为所有必填字段提供与 `FlightEvent` 一致的规范化依据；字段不足时每个缺失字段恰有一个追问；不支持输入不返回半成品事件。未知字段和被篡改的安全声明均被拒绝。
- 回退契约：模型与确定性规则来源不能混淆；未配置、超时、提供方错误和非法模型输出使用有限安全原因。`deterministic_only` 后续可完全禁用外部调用，AI 失败不影响 M4 核心闭环。
- 解释契约：场景解释绑定不可变版本，运行解释绑定 session、revision 和方案 ID；摘要、每项取舍和下一步均为独立 `ExplanationClaim`，每条 claim 必须引用响应中存在的权威 evidence ID。
- 目标边界：只预留均衡、关键任务优先、最小等待和最小变更四个枚举，建议固定 `applied_to_planner=false`。当前只有 `balanced` 对应既有确定性优化器，其他目标尚未实现。
- 验证：M5-0 专项 `11 passed in 0.40s`，后端全量 `144 passed in 7.18s`；`compileall`、`pip check` 和 `git diff --check` 通过。M4 的既有 133 项回归全部保持通过。
- 文档：M5 总计划见 `docs/m5-ai-assistance-plan.md`，冻结契约见 `docs/m5-ai-contract.md`，审查记录见 `docs/m5-00-review.md`。
- 安全与边界：继续只使用匿名化、历史回放或合成教学数据；不接真实机场生产流、个人信息、模型密钥或外部控制系统；完整安全声明保持不变。
- 下一步：只进入 M5-1，实现不依赖模型的确定性中文事件解析器和至少 10 条完整/缺失/歧义/不支持样例；只返回草稿或追问，不应用事件。

### 2026-08-05 / M5-1 确定性中文事件草稿

- 完成：新增纯规则中文解析器与 `POST /api/v1/assistant/event-drafts`，支持航班延误和登机口变更、绝对时间、绑定权威参考时间的相对时间、中文登机口名称、字段依据、缺失追问、冲突意图和不支持输入。
- 上下文：场景请求必须绑定当前 `expected_version` 与带时区参考时间；运行请求必须绑定当前 session/revision，参考时间只取权威仿真时钟，当前登机口只取运行航班投影。陈旧上下文分别返回 `assistant_version_conflict` 或 `assistant_revision_conflict`。
- 样例：14 条解析测试覆盖 4 条完整事件、7 条缺失/未知/歧义字段、2 条冲突或不支持意图，以及 1 条场景时间窗外输入；规则不能唯一核对航班、时间、延误分钟或新登机口时明确追问，不猜测。
- 只读与来源：响应只允许 `ready_for_review`、`needs_clarification`、`unsupported`，固定 `requires_human_confirmation=true`、`applies_automatically=false`。`auto` 在本单元以 `model_not_configured` 透明回退规则，`deterministic_only` 不声称模型回退；原始输入不回显或持久化。
- 无副作用：专项测试在请求前后比较场景快照、场景审计、SQLite 运行记录和运行审计，均完全一致；本单元不应用事件、不生成计划、不推进前端状态、不发起外部网络调用。
- 验证：M5-1 聚焦 `21 passed in 1.08s`，后端全量 `165 passed in 6.90s`；M4 的 133 项和 M5-0 的 11 项回归全部保持通过。
- 文档：M5 总计划已把 M5-1 标记完成；详细范围、审查和已知边界见 `docs/m5-01-review.md`。
- 安全与边界：未读取或保存模型密钥，不接真实机场生产数据、个人信息或外部控制系统；完整教学仿真安全声明和既有 M4 优点保持不变。
- 下一步：只进入 M5-2，实现可替换的 OpenAI-compatible/DeepSeek 结构化输出适配器；密钥仅从本地进程环境读取，模型未配置、超时、提供方错误或输出非法时必须回退当前确定性解析器，且仍不得自动应用事件。

### 2026-08-05 / M5-2 OpenAI-compatible 模型适配与安全回退

- 完成：新增提供方无关的 OpenAI-compatible chat-completions 适配器；正式应用只在进程环境完整提供 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 时启用，业务服务、规则解析器、API 和前端均不依赖 DeepSeek SDK 或提供方响应细节。
- 最小发送：外部请求只包含当前匿名文本、权威参考时间/时间窗、航班 ID/展示号/当前登机口和可选登机口，不发送场景 ID、运行 session/revision、SQLite、审计、方案、本机路径或其他会话数据；密钥仅进入 Authorization 请求头。
- 结构化防线：模型只抽取有限事件类型、权威实体 ID 和最短原文片段，不生成事件 ID、状态或计划。后端再次核对 quote 确实来自输入、航班/登机口属于权威上下文、已知意图未被反向分类、数字分钟一致、相对时间仍由确定性解析器按权威参考时间换算。
- 回退：`deterministic_only` 对已配置提供方也保持零调用；`auto` 的未配置、超时、HTTP/传输错误和非法/冲突输出分别公开 `model_not_configured`、`model_timeout`、`provider_error`、`invalid_model_output`，并返回 M5-1 规则结果，不返回原始异常、响应体、URL、密钥或内部提示词。
- 验证：M5-2 专项 `19 passed in 1.05s`，M5 聚焦 `51 passed in 1.54s`，后端全量 `184 passed in 6.99s`；`compileall`、`pip check`、`git diff --check` 通过，M4 的 133 项回归全部保持。
- 密钥状态：本轮没有把用户提供的密钥写入源码、文档、测试、命令、日志或 Git；当前进程环境未配置 `AI_*`，因此未进行真实外部调用，正式服务在无模型时继续稳定使用规则回退。
- 安全与边界：模型输出仍固定待人工确认且不自动应用；本单元不实现事件提交、目标配置、方案解释、前端 AI 交互或真实机场生产接入。
- 文档：详细实现、安全审查和已知边界见 `docs/m5-02-review.md`。
- 下一步：只进入 M5-3，实现事件草稿人工复核提交与有限确定性调度目标配置；提交继续走 expected version/revision、Pydantic、CP-SAT、独立硬约束检查和人工确认，模型不能直接调用规划器。

### 2026-08-06 / M5-3 人工复核提交与确定性目标配置

- 完成：新增 `POST /api/v1/assistant/event-drafts/submit`。客户端必须回传完整可复核草稿并显式确认事件；场景事件复用 `ScenarioService.apply_events()` 和 `expected_version`，运行事件复用 SQLite revision、权威仿真时间、既有事件边界和滚动重规划服务，不建立服务器草稿库或第二状态机。
- 运行事件：只允许在 `ready` 或 `paused` 提交，且必须显式确认四选一的规划目标。当前时刻事件立即原子应用并生成候选；未来事件只登记到运行投影，等待权威时钟到达后生成自己的唯一候选。重复事件、时间倒退、陈旧 revision 和实体不匹配均安全拒绝且不改状态。
- 目标配置：新增 `balanced`、`critical_first`、`minimum_wait`、`minimum_change` 四种确定性词典序。旧 CP-SAT 请求继续精确使用原 `balanced` 算法名、计划 ID 和结果；显式目标必须人工确认。`minimum_change` 仅允许以当前执行方案为资源安排基线的滚动规划，静态请求返回 `planning_objective_not_supported`。
- 状态所有权：`objective_profile` 随 `Plan` 和 SQLite `RuntimeProjectionSource` 持久化并在运行快照公开；后续自动事件和人工重规划使用会话当前目标，重置恢复初始方案目标。旧计划与旧 SQLite JSON 缺字段时默认 `balanced`。所有目标仍走 CP-SAT 和独立 `validate_plan`，候选绝不自动采用。
- 兼容性修正：全量回归发现新增计划字段改变了旧 `/api/v1/demo` 响应。现由离线演示专用兼容序列化排除该新字段，保留 M2/M3 静态 JSON 逐字契约；M5 业务计划与运行响应仍正常公开目标。
- 验证：M5-3 专项 `12 passed in 1.57s`，M4/M5 聚焦回归 `124 passed in 5.07s`，后端全量 `196 passed in 8.24s`，前端 `19 passed`，`compileall`、`pip check`、类型检查和生产构建通过；构建产物 CSS 55.11 kB、JS 281.89 kB。
- 密钥与数据：没有复述、写入、输出或提交用户提供的密钥；自动化全部使用本地假提供方或确定性规则，没有进行真实外部调用。继续只使用匿名合成教学数据，不接真实机场生产流、个人信息或控制系统。
- 文档：详细实现、安全审查、错误语义和已知边界见 `docs/m5-03-review.md`；M5 总计划和 AI 契约已同步。
- 下一步：只进入 M5-4，基于当前权威场景版本或运行 revision 构造有限方案证据和可追溯解释；每条结论必须引用存在的 evidence ID。不得提前实施 M5-5 前端交互、自动采用候选或真实生产接入。

### 2026-08-06 / M5-4 权威方案事实与可追溯解释

- 完成：新增只读 `POST /api/v1/assistant/plan-explanations`，支持不可变场景方案和运行当前/候选方案；运行上下文通过 `RuntimeSessionService.get_explanation_context()` 将请求 revision、快照和持久化投影事实绑定在同一读取边界。
- 事实链：后端确定性构造方案状态、目标、指标、任务分配、未分配原因、硬约束、事件状态和当前/基线差异；摘要、取舍与建议下一步全部带 `FACT-*` 引用，模型不得生成事实 ID 或自由排班。
- 可选 AI：使用共享 OpenAI-compatible HTTP 适配器，环境变量仅为 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。无配置时确定性解释完整可用；模型超时、提供方错误、非法结构、不完整 claim 或未知 evidence 引用均回退规则解释。
- 只读与状态所有权：解释请求不推进仿真时间、不应用事件、不生成或采用候选、不写审计、不增加 revision；所有响应固定 `modifies_plan=false`、`requires_human_confirmation=true` 并携带完整安全声明。
- 验证：M5-4 专项 `13 passed`；后端全量 `209 passed in 9.00s`；前端 `19 passed`、类型检查和生产构建通过；`compileall`、`pip check`、`git diff --check` 和敏感信息扫描通过。上一已提交基线为 `fe6ad61 feat: add reviewed M5 event submission`，本单元按独立提交收口。
- 安全与部署：未写入用户提供的真实密钥，不接真实机场生产数据或个人信息；AI 是可选表达增强，不是 GitHub 克隆、公网部署或核心闭环的启动前置条件。M6 负责部署版本，M7 负责稳定公网地址。
- 详细计划与审查：[m5-04-plan.md](m5-04-plan.md)、[m5-04-review.md](m5-04-review.md)。
- 下一步：只进入 M5-5，把事件草稿、人工复核、目标确认和方案解释接入现有五页共享前端状态；不得创建第二运行状态、自动采用候选或接入真实机场生产流。

### 2026-08-07 / M5-5 前端辅助交互接入

- 完成：“事件影响”页新增匿名自然语言事件录入、`auto`/`deterministic_only` 模式、缺失字段追问、结构化字段与逐字段来源、四选一确定性目标、双重人工确认和人工提交；“方案依据”页新增当前/候选切换、四种解释重点、匿名补充问题、来源透明度、事实结论和 `FACT-*` 引用展示。
- 状态所有权：草稿固定绑定当前 `session_id + revision`，解释固定绑定 session、revision、方案、基线、重点、问题与模式；输入或权威上下文变化后旧结果立即过期。提交结果经现有 `commitSnapshot` 进入唯一共享快照，不增加第二时钟、第二规划器、第二候选状态或自动重放。
- 人工边界：草稿生成不写业务状态；只有事件字段和有限目标分别确认后才可提交。浏览器提交匿名 SIM330 延误事件后，权威快照由 R18/V3 更新为 R20/V4 并生成待确认候选，当前方案未被自动替换；人工“保留当前方案”后更新为 R21，候选清空，旧候选解释明确过期。
- 无模型闭环：未配置模型时页面明确显示规则回退，缺失时间和延误分钟数会逐项追问；完整输入可完成草稿、人工提交、CP-SAT 候选和当前/候选事实解释。解释摘要、取舍和建议均展示有效 `FACT-*` 引用。
- 布局与浏览器：Microsoft Edge 在 1440x900 与 390x844 完成事件和方案页真实检查；文档宽度等于视口宽度，无横向溢出，事件/方案卡片没有内部纵向滚动，只有整页工作区滚动；控制台 0 error / 0 warning，完整安全声明可见。截图位于忽略提交的 `output/playwright/m5-5/`。
- 自动验证：前端 3 个测试文件、`24 passed`，`npm run typecheck` 与生产构建通过，产物 CSS 64.45 kB、JS 302.14 kB；后端全量 `209 passed in 9.32s`；`compileall`、`pip check`、`git diff --check` 和前端产物敏感信息扫描通过。
- 安全与部署：前端只调用同源 `/api/v1`，不包含 API key、提供方 URL、硬编码模型名或专用 SDK；未写入用户提供的真实密钥，不接真实机场生产数据、个人信息或外部控制系统。无 AI 配置时 GitHub 克隆和后续公网部署仍可运行完整确定性核心闭环。
- 详细计划与审查：[m5-05-plan.md](m5-05-plan.md)、[m5-05-review.md](m5-05-review.md)。
- 下一步：M5-6 是唯一入口，只进行模型/无模型、提供方故障、提示词注入、浏览器和三轮连续总验收；不得提前进入 M6、发布 GitHub/公网地址或接入真实机场生产流。

### 2026-08-08 / M5-6 AI 辅助总验收

- 总验收：新增 `tests/test_m5_acceptance.py`，通过公开 REST API 在同一 session 连续验证纯规则、假模型成功、越权文本与非法模型输出三轮完整闭环；每轮均先证明未确认不改状态，再经双重确认产生候选，最后分别保留、采用、保留。
- 故障与防护：事件草稿和方案解释均覆盖超时、提供方错误与非法结构安全回退；模型未知实体、非法 `FACT-*` 引用及“立即执行、自动采用”等注入文本不能越过权威实体、原文 quote、version/revision、确定性目标、硬约束和人工决定。
- 真实浏览器：Microsoft Edge 同一会话 `RUN-BAE9BD55F15E4CBC` 从 R12/V3 连续更新到 R21/V6，新增三条不同事件，第二轮明确采用最少等待候选，第一/三轮保留当前方案；最终 `paused`、9 项事件、当前方案为已人工采用的 `R16-CP-SAT-MINIMUM-WAIT`、候选为空。
- 前端证据：智能辅助在模型未配置时准确显示规则回退；越权事件文本仍生成待人工复核草稿，越权解释问题仍只返回后端 `FACT-*` 并建议人工决定。1440x900 与 390x844 无横向溢出，只有整页工作区滚动；控制台 3 条信息、0 error、0 warning，完整安全声明可见。
- 自动验证：M5-6 专项 `4 passed`，M5/AI 聚焦 `99 passed, 114 deselected`，后端全量 `213 passed in 10.17s`；前端 `24 passed`，类型检查、生产构建、`compileall`、`pip check`、`git diff --check` 和敏感信息扫描全部通过。
- 密钥边界：本次启动进程没有 AI 环境配置，未执行真实 DeepSeek 网络冒烟；当前执行面没有合规秘密注入通道，因此没有把对话密钥写入命令、工具参数、文件、日志或 Git。真实调用须由用户在可见本机终端或部署 secret manager 中注入三个后端环境变量后重启，并以 `trace.source=language_model` 核验。
- 安全与范围：只使用匿名合成教学数据，不接真实机场生产流、个人信息或外部控制系统；未进入 M6，未创建 GitHub 远程、推送或发布公网地址。
- 详细计划与审查：[m5-06-plan.md](m5-06-plan.md)、[m5-06-review.md](m5-06-review.md)。
- 下一步：M5 已整体收口，停在 M6 前。收到用户明确授权后，M6 才可开展规模/性能测试、部署秘密配置和发布前准备，并继续保持无模型可运行与 AI 仅辅助的既有优点。

### 2026-08-08 / M6-0 规模与部署契约冻结

- 阶段计划：新增 `docs/m6-scale-deployment-plan.md`，将 M6 拆为 M6-0 至 M6-6，依次完成契约、确定性规模场景、有界规划、基准报告、体验收敛、部署版本和总验收；M7 才创建 GitHub 远程和稳定公网地址。
- 规模档位：冻结 `small=100/20/5/2s`、`medium=500/50/10/5s`、`large_aggregate=2000/200/20/15s`（任务/资源/区域/目标）。所有档位固定 `synthetic`，大型只是匿名聚合仿真压力档位。
- 严格契约：新增 `backend/app/scale_models.py`，定义规范档位、单次样本和报告。报告的 p50、nearest-rank p95、最大值、目标状态和回退状态必须从有序样本推导；所有计划完整覆盖任务且硬约束违规为 0。
- 回退边界：小型/中型只接受 CP-SAT；大型聚合只允许明确的 `model_size_guard` 或 `time_limit` 确定性回退。回退必须保留实际执行路径、待协调任务和方案指标，不能伪装成 CP-SAT 成功。
- Review 修正：将基准平台从自由文本收紧为 `windows/linux/macos`，Python/OR-Tools 版本限制为数字格式，未知主机名、本机路径、额外字段和安全声明覆盖均拒绝。
- 验证：M6-0 专项 `5 passed in 0.43s`，后端全量 `218 passed in 10.20s`；前端 `24 passed`、类型检查和生产构建通过；`compileall`、`pip check`、`git diff --check` 和 key-like 扫描通过。
- 安全与事实：本单元未生成规模场景、未运行性能基准或修改调度器，因此没有声称达到 2/5/15 秒目标；未读取或调用真实模型密钥，不接真实机场数据、个人信息或控制系统。
- 详细计划、契约与审查：[m6-scale-deployment-plan.md](m6-scale-deployment-plan.md)、[m6-scale-contract.md](m6-scale-contract.md)、[m6-00-review.md](m6-00-review.md)。
- 下一步：只进入 M6-1，实现确定性规模场景工厂、稳定指纹以及 100/500/2000 档位的数据完整性测试；不得提前发布性能结论、静默回退、部署公网或接入生产数据。

### 2026-08-08 / M6-0B 真实 AI 激活

- 真实模型入口：新增 `配置AI.cmd` 与 `scripts/configure-ai.ps1`。用户在本机遮蔽输入 API key、OpenAI-compatible base URL 和模型名；密钥以当前 Windows 用户 DPAPI 密文保存于忽略提交的 `.runtime/ai-config.json`，不会进入仓库、前端、日志或响应。
- 启动所有权：`start-project.ps1` 在后端子进程启动前解密并短暂注入 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`，启动完成后恢复父进程环境；运行状态只保存配置文件指纹。配置变化会停止并重建现有服务组，避免旧的无模型后端继续运行。
- 模型选择：依据 DeepSeek 官方当前 OpenAI-compatible 文档，默认 `deepseek-v4-flash`；它与 `deepseek-v4-pro` 均支持 JSON Output。模型名仍可在配置窗口修改。
- 保留边界：模型只负责事件字段抽取和权威事实表达增强；原文引用、航班/登机口实体、时间、四选一调度目标、硬约束和人工候选决定仍由后端确定性链路拥有。无模型配置、模型超时、提供方错误或非法输出继续回退规则。
- 部署边界：`.env.example` 只定义变量名和非秘密默认值；GitHub/公网部署使用平台 Secret Manager 或进程环境，不使用 DPAPI 本机文件，不接真实机场生产数据或外部控制系统。
- 真实冒烟已完成：匿名事件 `SIM102 在 08:12 确认延误 20 分钟` 返回 HTTP 200，草稿状态为 `ready_for_review`，`trace.source=language_model`、`trace.provider_attempted=true`、模型为 `deepseek-v4-flash`，无回退原因；方案 `PLAN-TERMINAL-DISTURBANCE-01-V1-CP-SAT` 的解释也返回 `trace.source=language_model`，包含 20 条权威证据、8 个合法 `FACT-*` 引用，`modifies_plan=false` 且 `requires_human_confirmation=true`。
- 兼容性修复：DeepSeek V4 结构化请求显式禁用默认 thinking；OpenAI-compatible 响应信封改为忽略合法标准元数据，业务输出仍严格拒绝未知字段。浏览器页面显示“模型辅助解释 · deepseek-v4-flash”和“仍需人工确认”，控制台 0 error / 0 warning。
- 自动验证：后端全量 `219 passed in 10.17s`；前端 3 个测试文件、`24 passed`；类型检查、Vite 生产构建、`compileall`、`pip check`、全部 PowerShell 语法与纯 ASCII 检查、`git diff --check` 和仅计数的 key-like 扫描均通过；`.runtime/ai-config.json` 未被 Git 跟踪。
- 阶段收口：M6-0B 已完成；下一入口仍为 M6-1 确定性规模场景工厂与 100/500/2000 档位的数据完整性测试。M6-1 未开始，不发布性能结论、不接入真实机场生产流、不创建 GitHub 远程或部署公网。
- 详细审查：[m6-00b-real-ai-activation-review.md](m6-00b-real-ai-activation-review.md)。

### 2026-08-08 / M6-0A 一键启动陈旧 PID 恢复修正

- 根因：`.runtime/server-state.json` 残留已结束服务的 PID，其中一个 PID 后续被无关 Windows 进程复用。旧启动脚本能发现进程身份不一致并避免误杀，但把安全的 PID 复用与损坏状态一并视为不可恢复冲突，因此无法清理状态并重新启动网页。
- 修复：启动/停止脚本把状态分为 `valid`、`missing`、`reused` 和 `invalid`。进程名、启动时间或可执行路径不匹配属于 `reused`，绝不停止该进程；状态字段本身无法可信解析才属于 `invalid` 并继续阻断自动处理。
- 恢复：全部记录均为 `missing/reused` 时直接清理陈旧状态并启动新服务；部分有效或整组不健康时，只停止通过项目根目录、服务名、PID、进程名、启动时间和可执行路径严格校验的本项目进程，再重建完整服务组。
- 动态证据：真实陈旧状态修复后成功启动后端 PID `17916`、前端 PID `3516`，重复启动复用同一组 PID；停止入口精确结束有效项目进程并删除状态文件，原已复用的无关 PID `18472` 在启动、停止前后均保持存活。
- 自动验证：PowerShell 脚本语法、后端全量 `218 passed in 10.43s`、前端 `24 passed`、`npm run typecheck`、生产构建、`compileall` 和 `pip check` 通过。Vite/Vitest 在受限沙箱内因子进程权限返回 `spawn EPERM`，在获准的普通进程环境复测正常。
- 范围：本次是 M6-0 后的本地启动可靠性维护，不进入 M6-1，不修改实时状态所有权、调度器、AI 边界、机场数据边界、GitHub 远程或公网部署计划。
- 详细审查：[m6-00a-startup-recovery-review.md](m6-00a-startup-recovery-review.md)。下一阶段入口仍为 M6-1。

### 2026-08-08 / M6-1 确定性规模场景工厂

- 完成：新增 `backend/app/scale_scenario_factory.py`，从 `small`、`medium`、`large_aggregate` 三个规范档位生成 100/500/2000 项任务、20/50/200 项资源、5/10/20 个区域和 10/50/200 个匿名航班的严格 `Scenario`。
- 纯函数边界：工厂只使用固定 `2026-08-01` 教学日期和确定性编号/时间规律，不读取网络、系统时间、真实数据、AI 配置、运行会话或用户输入；场景固定为 `simulation + synthetic` 且事件集为空。
- 完整性：每个区域包含到全部区域的对称路网；所有任务的航班、起终区域引用闭合，任务/资源时间落在固定 12 小时窗口，三种资源类型和四种任务类型均被覆盖，每项任务存在类型与容量匹配资源。
- 指纹：新增严格 `ScaleScenarioArtifact`，绑定规范档位、场景和规范 JSON 的 SHA-256。三个档位分别冻结为 `7dc3827d...bcf11`、`5775f0c5...3d649`、`a5e0bae3...9c70e`；连续生成和 JSON 往返保持一致，内容篡改携带旧摘要会被拒绝。
- 专项验证：M6-0/M6-1 聚焦 `18 passed in 0.77s`，覆盖精确计数、固定摘要、调用隔离、JSON 往返、引用、路网、时间窗、资源类型/容量、篡改和未知档位拒绝。该耗时不是规划基准，未产生 2/5/15 秒性能结论。
- 全量验证：后端 `232 passed in 10.86s`；前端 3 个测试文件、`24 passed`；类型检查、Vite 生产构建、`compileall`、`pip check`、PowerShell 语法、新代码纯 ASCII、`git diff --check` 和仅计数的 key-like 扫描通过。
- 范围：未调用或修改 FIFO、CP-SAT、`validate_plan()`、公开 API、前端、SQLite 或 AI 链路，不生成基准样本、性能报告或回退结果，不接真实机场生产流或部署公网。
- 详细审查：[m6-01-review.md](m6-01-review.md)。下一唯一入口为 M6-2 有界可扩展规划与显式安全回退；M6-3 前不得发布性能结论。

### 2026-08-08 / M6-2 有界规模规划与安全回退

- 完成：新增 `backend/app/scale_planner.py` 和严格 `ScalePlanningResult`。小型/中型规范场景走 `bounded_scale_cp_sat_v1`；大型在创建 CP-SAT 模型前走 `deterministic_scale_fallback_v1`，公开 `model_size_guard`。
- 模型估算：三档兼容对为 750/9,375/150,000，有界候选对为 300/1,500/6,000，旧全量排序对为 15,250/971,875/62,425,000；固定 guard 为 500 项任务、1,250,000 排序对和每任务最多 3 个候选资源。
- 有界求解：小/中使用可选区间与 `AddNoOverlap` 代替同资源任务对两两先后变量，预留全场最大行程缓冲；确定性安全起始解只作为 CP-SAT hint，最终任务书读取求解器结果并按真实最短路重建。
- 安全结果：三档功能验证均为 `executable`，完整分配 100/500/2000 项任务，独立 `validate_plan()` 违规为 0。小/中超时直接失败、不调用回退；大型 guard 测试证明建模函数零调用。
- 契约修正：只把求解器 `UNKNOWN` 识别为 `time_limit`；`INFEASIBLE`、`MODEL_INVALID`、代码错误和复核失败不能伪装成回退。结果严格绑定冻结指纹、规范场景 ID/version、列表/指标、路径/原因、人工确认和安全声明。
- 指纹加固：M6-1 工件现在不仅校验内容摘要一致，还必须等于该档位冻结摘要；篡改后重新计算新摘要仍被拒绝，不能静默替换 M6-3 基准输入。
- 专项验证：M6-0 至 M6-2 聚焦 `28 passed in 6.43s`。该时间包含场景生成、多次功能求解和断言，不是正式规划样本，未产生 2/5/15 秒、p50/p95 或最大值结论。
- 全量验证：后端 `242 passed in 16.56s`；前端 3 个测试文件、`24 passed`；类型检查、Vite 生产构建、`compileall`、`pip check`、PowerShell 语法、新代码纯 ASCII、`git diff --check` 和仅计数的 key-like 扫描通过。
- 范围：未修改现有业务优化器、公开 API、实时 session/revision、SQLite、SSE、前端或 AI；不接真实机场生产流、不创建 GitHub 远程或部署公网。
- 详细审查：[m6-02-review.md](m6-02-review.md)。下一唯一入口为 M6-3 离线基准运行器、FIFO/有界规划对比和从实际样本生成的严格性能报告。

### 2026-08-09 / M6-3 离线基准与 FIFO/有界规划对比

- 完成：新增 `backend/app/scale_benchmark.py` 与 `scripts/run_scale_benchmark.py`，在三个 M6-1 冻结场景上按固定 1 次预热、每档 5 组 FIFO/有界配对样本生成严格 `ScaleBenchmarkSuite` 和可提交 JSON 报告。
- 完整计时：样本从已验证场景进入规划开始，到任务书完成独立 `validate_plan()` 为止；场景生成、JSON、网络、浏览器、AI 和依赖冷启动不计入。小/中/大型固定 1/3/9 秒求解预算，为 2/5/15 秒完整入口预留构建和复核时间；大型 guard 不实际进入求解器。
- 正式结果：Windows、Python 3.13.9、OR-Tools 9.15.6755、24 逻辑 CPU、单 worker 下，小/中/大型有界入口 p50 为 1.022186/3.111318/2.747334 秒，p95/最大值为 1.022711/3.113737/2.776355 秒，三档均达标。
- 路径与安全：小型、中型 5 个样本全部走 `cp_sat` 且无回退；大型 5 个样本全部在建模前走 `deterministic_fallback + model_size_guard`。全部 30 个 FIFO/有界正式样本完整覆盖 100/500/2000 项任务，任务和关键任务完成率 100%，硬约束违规为 0。
- 质量修正：M6-3 冒烟发现把全部目标时间交给求解器会使完整入口略超 2/5 秒，已用规范求解预算保留复核余量；同时按 CP-SAT 选定资源/顺序和真实最短路把任务书左移到最早可行时间，消除保守最大行程缓冲造成的可避免空闲，并重新独立复核。小/中型算法因此明确升级为 `bounded_scale_cp_sat_v2`，不沿用 v1 名称隐藏语义变化。
- FIFO 对比：冻结场景资源充足、释放均匀，FIFO 平均等待为小型 0.00、中型 0.03 分钟，低于有界路径的 0.57/1.11 分钟；大型两条路径均为 0。该真实取舍完整保留，不能宣称 CP-SAT 在当前样本的速度或等待质量上优于 FIFO，也不通过替换冻结场景或隐藏样本制造优势。
- 严格证据：报告绑定场景 ID/version/冻结指纹、规范求解预算、有限环境字段、连续样本、计划指标、第一组配对对比、p50/nearest-rank p95/最大值、目标和回退状态；未知字段、主机名、本机路径、密钥和安全声明覆盖均被拒绝。
- 自动验证：M6-0 至 M6-3 聚焦 `38 passed in 20.77s`，后端全量 `252 passed in 29.24s`；前端 3 个测试文件 `24 passed`，类型检查和 Vite 生产构建通过；`compileall`、`pip check`、PowerShell 语法、Git 差异和敏感信息检查通过。
- 范围：运行器不访问网络、真实模型配置、SQLite、实时 session/revision 或公开 API；未修改前端、AI 链路或真实业务状态，不接真实机场生产流，不创建 GitHub 远程或部署公网。
- 正式证据：[m6-benchmark-report.json](m6-benchmark-report.json)；详细审查：[m6-03-review.md](m6-03-review.md)。下一唯一入口为 M6-4 大数据相关 API 与五页工作台体验收敛。

### 2026-08-09 / M6-4 大数据相关 API 与五页工作台体验收敛

- 权威摘要：`RuntimeSessionSnapshot` 新增严格派生的 `collection_summary`，覆盖任务总数/活动/需关注/完成/锁定、资源总数/活动、事件总数/开放/待发生和航班总数；它随现有 REST/SSE 快照传输，不新增持久化状态或平行计数 API。
- 有界展示：任务、事件、航班、资源、区域支持当前快照检索并分别按 50/20/24/40/20 项分页；受影响任务按 30 项、当前/候选任务书各按 25 项、候选差异按 20 项分页，始终公开当前范围和完整总数。
- 所有权：检索、筛选、页码和选中项只属于浏览器展示状态，不增加 revision、不推进仿真时间、不调用规划器或 AI。从资源或证据跳转到任务时会清除冲突检索并定位目标页，仍使用同一共享快照。
- 加载恢复：初始化界面说明匿名场景、SQLite 会话和权威状态流三个恢复步骤但不伪造百分比；连接提示可分别读取最新权威快照或重新连接，revision 冲突仍不自动重放旧命令。
- 移动与方案：390×844 下任务表变为字段卡片、资源列表变为两列卡片；方案任务书、候选差异和受影响任务按内容自然展开，当前方案继续显示中文算法、用途、资源、时段、路线和指标，方案 ID 只是次级审计字段。
- 大集合证据：纯函数验证 2000 项、每页 50 项和 40 页；React 服务端渲染验证 30 项方案任务书只渲染 25 项、30 项候选变化只渲染 20 项，同时总数不丢失。
- 浏览器证据：Microsoft Edge 在 1440×900 与 390×844 完成五页检查；桌面/移动文档宽度分别等于 1440/390，移动任务和资源列表 `scrollWidth/clientWidth` 均为 `359/359`，桌面方案任务书 `scrollHeight/clientHeight` 为 `1143/1143`；控制台 3 条信息、0 error、0 warning，完整安全声明可见。
- 自动验证：后端集合/API 聚焦 `23 passed`，后端全量 253 项以退出码 0 完成；前端 5 个测试文件、`32 passed`，类型检查和 Vite 构建通过；`compileall`、`pip check`、PowerShell 语法、Git 差异和 key-like 扫描通过。标准启动脚本恢复真实 DeepSeek 后端配置，health 为 `ok`、前端 HTTP 200，但本单元未发起真实模型网络调用。
- 范围：未修改 M6-3 冻结场景、正式样本或报告，不接真实机场生产流、个人信息或外部控制系统，不创建 GitHub 远程或发布公网地址。
- 详细审查：[m6-04-review.md](m6-04-review.md)。下一唯一入口为 M6-5 provider-neutral 单 origin 部署版本；只处理同源 API、单 worker、SQLite 持久化卷、非 root、健康检查、请求限制、错误脱敏与平台秘密注入，不提交本机 DPAPI 配置，也不提前进入 M7 发布。

### 2026-08-09 / 机场空间态势与路径可视化规划

- 用户目标：在机场平面图或立体图上实时定位不同突发情况，并把当前方案、候选方案、资源移动和解决流程直接可视化，使演示更接近真实机场运行观感。
- 可行性：现有任务已包含起终区域，方案包含资源/时段/路线，资源投影包含 `from/to/progress_pct`，事件和航班包含登机口与状态，SSE/REST 已能提供权威实时更新；缺口是区域坐标、路径折线、楼层和资产许可证，不能从通行时间或 AI 文本猜测。
- 技术决策：第一版使用原创匿名放射式枢纽机场 SVG，在调度总览内增加空间态势和放大模式，保持现有五页与共享选择；当前方案为实线、待确认候选为虚线，资源位置严格绑定后端进度，暂停/断线时不自推进、不循环、不随机。
- 资产边界：只有来源、授权和公开发布范围明确时才允许导入类似北京大兴机场的 SVG/GeoJSON/glTF；禁止商业地图截图、内部运行图、真实坐标、安防/限制区细节和未知来源资产。默认始终标注“匿名机场仿真空间示意图”。
- 阶段顺序：当前下一入口仍为 M6-5 单 origin 部署，不被新增想法打断；M6-5 后新增 M6-5A，依次完成空间契约、原创布局、后端权威 overlay、2D 联动和连续事件验收，再进入 M6-6。3D 是可选渐进增强，不作为首次 GitHub/公网发布前置条件。
- 状态与 AI：空间层不新增时钟、规划器、候选或业务状态；AI 不能猜坐标、路径或自动执行方案，只可继续做已有限制的事件抽取与事实解释。
- AI 实用性补充：空间态势必须提供绑定当前 session/revision、仿真时间和地图选择的实际问答，覆盖事件位置、影响范围、活动资源、当前/候选路线、处理阶段和下一权威边界；回答必须引用后端 `SPATIAL-FACT-*`，可返回经白名单校验的地图聚焦 ID。无模型时规则回答仍可用，模型不能成为脱离运行进程的空放聊天框。
- 详细计划：[airport-spatial-visualization-plan.md](airport-spatial-visualization-plan.md)。本记录只完成规划，尚未实施 M6-5A，也未导入任何机场图或真实生产数据。
